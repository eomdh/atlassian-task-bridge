# AI 활용

이 프로젝트는 에이전트 기반으로 개발하고 있다. 이 문서는 무엇을 맡겼고, 무엇을 직접
판단했고, 결과를 어떻게 검증했는지를 남긴다.

진행 중인 문서다. 3장의 슬라이스 기록이 작업이 쌓이는 만큼 이어서 늘어난다.
작업이 끝난 뒤에 몰아서 쓰지 않고 각 작업 커밋에 함께 담는다. 에이전트 제안을 반려하거나
방향을 바꾼 경우도 그날 남긴다.

## 1. 도구

| | |
|---|---|
| 도구 | Claude Code (CLI) |
| 모델 | Claude Fable 5, Opus 5 |
| 플러그인 | SuperClaude, oh-my-claudecode |
| 맡긴 것 | 코드 작성, 배포와 설치 실행, 플랫폼 문서 조사, 문서 초안 |
| 맡기지 않은 것 | 주제 선정, 설계 결정, 작업 단위 승인, 화면 동작 확인, 문서 확정, 커밋 시점 판단 |

## 2. 역할 경계

작업을 슬라이스로 나누고, 슬라이스마다 에이전트가 범위와 방식을 제안하면 승인한 뒤에
진행한다. 코드는 에이전트가 쓰되 결정마다 왜 그렇게 했는지 그 자리에서 설명하게 하고,
납득되지 않으면 되돌린다. 이 규칙은 `CLAUDE.md` 의 `작업 방식` 섹션에 있다.

**직접 하는 것**

- 주제 선정과 설계 결정
- 슬라이스 범위 승인
- 데모 사이트에서 화면 동작 확인
- 문서 확정. 초안은 에이전트가 쓰고 내용을 검토해 고친 뒤 커밋한다
- 커밋 시점 판단과 지시

**에이전트에 맡기는 것**

- 코드 작성과 수정
- `forge lint`, `deploy`, `install`, `logs` 실행
- 플랫폼 문서 조사와 정리
- 문서 초안

핵심 로직인 ADF 평문 변환은 코드를 쓰기 전에 규칙을 먼저 정했다 (`DECISIONS.md` 1.7).
규칙은 데모 사이트에서 직접 측정한 응답에서 도출했고, 에이전트는 그 규칙대로 구현한다.

## 3. 슬라이스별 기록

### 슬라이스 0. Forge 스캐폴드 (`00d2269`)

**지시**: Forge 앱 스캐폴드 생성과 레포 배치.

**산출**: `forge create` 로 `confluence-content-action-ui-kit` 템플릿 생성. 템플릿이 만든
하위 디렉토리를 레포 루트로 올리고 무시 규칙 병합.

**판단**: 에이전트가 템플릿이 만든 하위 디렉토리를 그대로 두는 안과 루트로 올리는 안을 냈다.
관행에 어긋나는 배치가 아닌지 먼저 확인시켰고, 단일 앱 레포는 `manifest.yml` 을 루트에
두는 것이 일반적이었다. 앱이 하나뿐인 레포에서 forge 명령을 매번 한 단계 들어가서 칠 이유가
없어 루트로 올렸다.

**검증**: `forge lint` 통과. development 환경에 배포하고 데모 사이트 Confluence 에 설치해
왕복 확인.

**직접 확인**: 배포와 설치 명령의 성공 출력까지만 봤다. 이 시점의 화면은 템플릿의
hello world 라 Confluence 에서 열어보지 않았다. 화면을 실제로 연 것은 슬라이스 2 다.

### 슬라이스 1. 에이전트 지침 (`279cfc4`)

**지시**: 이전 프로젝트의 `CLAUDE.md` 를 참고해 커밋 규칙을 포함한 프로젝트 지침 정리.
문서 세 개가 요구 항목 여덟 개를 빠짐없이 담도록 역할 배정.

**산출**: Forge 플랫폼 제약, 명령어, 완료 기준, 작업 방식, 범위, 코드 규약, 커밋 규약,
문서 역할 분담.

**판단**: 템플릿이 `AGENTS.md` 를 함께 넣어줬다. Claude 를 쓰는데 다른 도구용 파일이 왜
생겼는지 물었고 (4.2), 내용의 절반은 값어치가 있고 절반은 이 레포에 남길 성격이 아니라
추려서 `CLAUDE.md` 로 옮기고 원본은 지웠다.

건진 것은 UI Kit 컴포넌트 제한, Storage 접근 경로, 스코프 변경 시 재설치 필요,
`--non-interactive` 플래그. 버린 것은 영어 장문 주석 지시와 말장난 지시.

**검증**: 금지 어휘와 em dash 검사 통과. 199줄로 권장 상한 이내.

**직접 확인**: `CLAUDE.md` 전문을 읽고 직접 수정했다. 이전 프로젝트의 지침을 그대로
복제하지 않도록 문서 역할 분담을 이 프로젝트에 맞게 다시 배정했다.

### 슬라이스 2. 리스크 실측

**지시**: 설계를 확정하기 전에 플랫폼이 실제로 무엇을 주는지 측정. 측정 코드는 커밋하지
않고 결과만 문서로 남긴다.

**방식**: 에이전트가 리졸버에 임시 측정 코드를 넣고 배포하면, Confluence 페이지에서 앱을
열어 실행하고 `forge logs` 로 결과를 수집했다. 데모 사이트의 시드 회의록은 액션 아이템이
전부 비어 있어서, 평문, 멘션, 링크, 서식, 긴 글자 다섯 종류를 넣은 페이지를 직접 만들어
측정했다.

**측정한 것과 결과**:

| 확인한 것 | 결과 |
|---|---|
| tasks API 의 `page-id` 필터와 ADF body | 동작. 단 `value` 가 객체가 아니라 JSON 문자열 |
| 태스크 객체 필드 | `id`, `localId`, `status`, `pageId`, `spaceId`, `assignedTo`, `body`, `dueAt` |
| `localId` 를 매핑 키로 쓸 수 있는지 | 불가. 시드는 `1`, 신규는 UUID 로 형식이 일정하지 않음 |
| 멘션과 담당자 연결 | 액션 아이템의 멘션이 `assignedTo` 에 accountId 로 채워짐 |
| 서식이 들어간 한 줄의 구조 | 조각 4개로 분리. 이어붙이지 않으면 제목이 잘림 |
| 빈 체크박스의 내용 | `placeholder` 조각. 거르지 않으면 안내 문구가 이슈 제목이 됨 |
| 앱 자격으로 태스크를 읽을 수 있는지 | 읽힘. 단 사이트 전체로는 사용자 61건 대 앱 5건 |

측정 없이 문서만 보고 설계했다면 최소 세 가지를 틀렸을 것이다. JSON 이중 인코딩, 조각 분리,
멘션 조각에 `text` 필드가 없다는 점이다.

**검증**: 측정 결과를 `DECISIONS.md` 에 옮기고 실측 표시. 측정 코드 원복 후 `forge lint`
통과와 워킹 트리 정리 확인.

**직접 확인**: Confluence 에서 앱을 세 번 실행했다. 첫 실행에서 시드 회의록의 액션 아이템이
전부 비어 있는 것을 화면에서 확인했고, 그래서 다섯 종류를 넣은 페이지를 직접 만들었다.
두 번째 실행에서 측정값이 그대로인 것을 보고 페이지를 편집만 하고 게시하지 않았다는 것을
찾아냈다. 게시 후 세 번째 실행에서 멘션과 서식이 반영된 응답을 얻었다.

### 슬라이스 3. ADF 평문 변환 스펙

**지시**: 구현 전에 변환 함수의 동작을 테스트로 고정. 함수 경계와 멘션 표기를 먼저 정하고
시작.

**판단**: 에이전트가 함수를 둘로 나누는 안과 하나로 합치는 안, 멘션의 `@` 를 남기는 안과
떼는 안을 냈다. 둘로 나누고 `@` 는 남기기로 했다. 근거는 `DECISIONS.md` 1.10.

**산출**: `toPlainText` 12건, `titleFromTask` 11건. 슬라이스 2 에서 측정한 응답 6건을
fixture 로 사용. Vitest 설치와 `npm test` 스크립트. 구현 파일은 만들지 않았다.

fixture 에 넣어둔 긴 항목(id 200)을 쓰는 케이스가 처음에 빠져 있었다. 제한을 넘는 경우만
있고 길지만 제한 이내인 경우가 없어서 추가했다.

**검증**: `npm test` 가 두 파일 모두 구현 모듈 미존재로 실패하는 것을 확인. 의도한 red
상태다. 빈 스텁을 두지 않은 이유는 `null` 을 기대하는 테스트가 우연히 통과할 수 있어서다.
`forge lint` 는 오류 없음. 배포본(3.2.0)에 실측용 스코프가 남아 있어 메이저 버전 승인 항목이
뜨는데, 슬라이스 5 에서 실제 스코프를 넣고 배포하면 정리된다.

**직접 확인**: `npm test` 를 직접 실행해 red 상태를 확인했다. 두 파일이 `Cannot find module`
로 실패하고 `Tests no tests` 가 뜬다. 테스트가 틀려서 실패한 것이 아니라 구현이 없어 수집
단계에서 멈춘 것이다. 슬라이스 4 에서 같은 명령이 23건 통과로 바뀌는지가 다음 확인 대상이다.

### 슬라이스 4. ADF 평문 변환 구현

**지시**: 슬라이스 3 의 테스트를 통과시킨다. 구현 전에 설계부터.

**판단**: 설계 단계에서 에이전트가 조각을 이어붙이는 방식에 함정이 있다고 보고했다.
문단 두 개를 붙이려면 구분자가 필요한데, 인라인 조각 사이에 같은 구분자를 넣으면
단어 가운데가 갈라진다. 서식이 단어 중간에 걸린 경우다.

기존 테스트 23건은 이 경우를 잡지 못했다. 규칙만 올바르게 구현하고 넘어가는 대신
테스트를 1건 추가해서 막기로 했다. 나중에 누가 규칙을 되돌려도 테스트가 잡는다.

근거는 실측에 있다. 측정한 조각이 `'굵게' | ' 와 ' | '기울임' | ' 섞기'` 였고 공백이 text
조각 안에 이미 들어 있었다. 그러므로 인라인끼리는 구분자를 넣지 않고, 블록 사이에만
공백 하나를 넣는다.

**산출**: `src/lib/adf-text.js` 의 `toPlainText`, `src/lib/task-title.js` 의 `titleFromTask` 와
`TITLE_MAX_LENGTH`. 테스트 24건 통과.

**검증**: `npm test` 24건 통과, `forge lint` 오류 0.

첫 실행에서 5건이 실패했다. 태스크 `id` 가 정수인 줄 알고 테스트를 짰는데 API 는
`"196"` 처럼 문자열로 준다. `DECISIONS.md` 1.5 에 "정수다" 라고 적어둔 것도 같이 틀렸다.
테스트를 문자열 비교로 고치고 문서를 정정했다 (4.5).

**직접 확인**: `npm test` 를 직접 실행해 24건 통과를 확인했다. 슬라이스 3 에서 본
`Tests no tests` 가 `Tests 24 passed` 로 바뀌었다. 구현 파일 두 개도 읽었다.

배포는 하지 않았다. 이 슬라이스는 Forge 코드와 manifest 를 건드리지 않는다. 테스트 파일이
배포본에 딸려가지 않는지는 슬라이스 5 에서 리졸버가 이 함수를 import 할 때 확인한다.

## 4. 에이전트가 틀렸던 사례

에이전트 산출물을 그대로 받지 않고 확인해야 하는 이유다. 어떻게 드러났는지를 같이 적는다.

### 4.1 문서 조사 기반 계획이 플랫폼 변경을 놓쳤다

작업 시작 전에 문서 조사로 세운 계획에 Developer Space 가입 단계가 없었다. Forge 는 이제
모든 신규 앱을 Developer Space 안에 만들어야 하는데, `forge create` 를 실제로 실행하면서
드러났다.

같은 계획에 개발 사이트를 웹에서 수동 생성한다고 적혀 있었으나, `forge site provision` 이
생겨서 시드 데이터가 포함된 사이트를 명령 한 번으로 받을 수 있었다.

계획을 실측 결과로 교정했다. 이후 설계 결정은 문서 근거와 실측 근거를 구분해서
`DECISIONS.md` 에 표시한다.

### 4.2 에이전트가 자기 동작에 대해 틀린 주장을 했다

템플릿이 넣어준 `AGENTS.md` 를 두고 에이전트가 "그대로 두면 영어 장문 주석을 달게 된다" 고
경고했다. Claude 를 쓰는데 왜 다른 도구용 파일이 영향을 주는지 물었다.

에이전트가 공식 문서를 확인한 결과 Claude Code 는 `AGENTS.md` 를 읽지 않고 `CLAUDE.md` 만
읽는다. 경고 자체가 틀렸다. 지침은 `CLAUDE.md` 로 옮겼다.

### 4.3 승인 없이 범위를 벗어났다

실측 단계에서 에이전트가 승인 없이 `manifest.yml` 을 스코프 6개짜리로 교체하고, 임시
웹트리거 모듈과 의존성을 추가했다. 모듈 키가 중복되어 `forge lint` 가 실패한 시점에 멈춰
세웠다. 슬라이스 단위로 나누어 커밋하며 진행하고, 결정과 지시에 근거해서만 작업하도록
방식을 바꿨다.

전부 원복시키고, 슬라이스 단위 승인과 커밋 시점을 `CLAUDE.md` 에 규칙으로 넣어
커밋(`279cfc4`)으로 고정했다.

```
- 슬라이스 하나 = 커밋 하나
- 슬라이스를 시작하기 전에 범위와 방식을 제안하고 승인을 받는다. 코드부터 쓰지 않는다
- 커밋은 사람의 지시로 진행한다. 알아서 커밋하지 않는다
```

### 4.4 비대화형 환경에서 대화형 명령을 반복 시도했다

에이전트의 셸에 TTY 가 없어 `forge login` 과 `forge create` 의 프롬프트가 뜨지 않았다.
`expect` 로 가짜 터미널을 붙여 우회하려다 두 번 연속 실패했다.

두세 번 실패하면 멈추고 보고하는 규칙에 따라 에이전트가 중단하고 넘겼다. 로그인은 환경변수
파일로, 앱 생성은 터미널에서 직접 실행했다. 이후 `--non-interactive` 플래그로 대부분의
명령이 해결된다는 것을 확인했다.

### 4.5 태스크 id 를 정수로 단정했다

슬라이스 2 의 측정 결과를 문서로 옮기면서 에이전트가 `DECISIONS.md` 1.5 에 "`id` 는 사이트
전역에서 고유한 정수다" 라고 적었다. API 는 `"196"` 처럼 문자열로 준다. 측정 데이터를 화면에
찍을 때 따옴표 없이 출력해서 타입을 확인하지 않고 넘어간 것이다.

슬라이스 4 에서 테스트가 잡았다. fixture 를 `id === 200` 으로 찾는 케이스 5건이 전부 `null` 을
받았다. 구현이 아니라 문서의 사실 오류였고, 테스트가 없었으면 매핑 키를 정수로 다루다가
역방향 조회에서 터졌을 것이다.

문서를 정정하고, 테스트의 fixture 조회에 못 찾으면 던지는 가드를 넣었다. 조용히 `undefined`
가 흘러 `null` 이 되는 대신 원인이 바로 보이게 했다.

### 4.6 진행 중인 문서를 완성본처럼 썼다

`DECISIONS.md` 초안에 아직 시작하지 않은 기능이 구현 범위로 적혀 있었고, 이 문서는
"개발했다" 로 끝나 있었다. 커밋 전에 지적해서 구현 현황 표로 바꾸고 미착수를 미착수로
표시했다.

## 5. 검증 장치

에이전트 산출물을 믿지 않고 확인하는 데 쓰는 것들이다. 아직 쓰지 않은 것은 언제부터
적용하는지 함께 적는다.

- **`forge lint`**: manifest 문법과 알려진 오류. 실제로 모듈 키 중복(4.3)을 여기서 잡았다
- **실제 배포와 설치**: 린트는 UI Kit 컴포넌트를 잘못 써서 렌더가 안 되는 것을 잡지 못한다.
  화면과 트리거를 건드린 작업은 데모 사이트에 배포해서 눈으로 확인한다
- **`forge logs`**: 배포된 앱의 실제 응답 확인. 슬라이스 2 의 측정 데이터가 전부 여기서 나왔다
- **단위 테스트** (슬라이스 3부터 적용): ADF 평문 변환은 실패하는 테스트를 먼저 커밋한 뒤
  통과 커밋을 쌓는다. 스펙을 먼저 확정하기 위함이다
- **공식 문서 대조**: 에이전트가 플랫폼 동작을 주장하면 문서로 확인시킨다. 4.2 가 그 사례다
- **결정 설명**: 코드를 쓸 때 결정마다 이유를 설명하게 하고 슬라이스 기록에 남긴다.
  나중에 코드를 읽을 때 왜 그렇게 됐는지 추적하기 위함이다
