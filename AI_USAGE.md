# AI 활용

이 프로젝트는 에이전트 기반으로 개발하고 있다. 이 문서는 무엇을 맡겼고, 무엇을 직접
판단했고, 결과를 어떻게 검증했는지를 남긴다.

진행 중인 문서다. 3장의 슬라이스 기록이 작업이 쌓이는 만큼 이어서 늘어난다.
작업이 끝난 뒤에 몰아서 쓰지 않고 각 작업 커밋에 함께 담는다. 에이전트 제안을 반려하거나
방향을 바꾼 경우도 그날 남긴다.

## 1. 도구

| | |
|---|---|
| 도구 | Claude Code (CLI) |
| 모델 | Claude Fable 5, Opus 5 |
| 플러그인 | SuperClaude, oh-my-claudecode |
| 맡긴 것 | 코드 작성, 배포와 설치 실행, 플랫폼 문서 조사, 문서 초안 |
| 맡기지 않은 것 | 주제 선정, 설계 결정, 작업 단위 승인, 화면 동작 확인, 문서 확정, 커밋 시점 판단 |

## 2. 역할 경계

작업을 슬라이스로 나누고, 슬라이스마다 에이전트가 범위와 방식을 제안하면 승인한 뒤에
진행한다. 코드는 에이전트가 쓰되 결정마다 왜 그렇게 했는지 그 자리에서 설명하게 하고,
납득되지 않으면 되돌린다. 이 규칙은 `CLAUDE.md` 의 `작업 방식` 섹션에 있다.

**직접 하는 것**

- 주제 선정과 설계 결정
- 슬라이스 범위 승인
- 데모 사이트에서 화면 동작 확인
- 문서 확정. 초안은 에이전트가 쓰고 내용을 검토해 고친 뒤 커밋한다
- 커밋 시점 판단과 지시

**에이전트에 맡기는 것**

- 코드 작성과 수정
- `forge lint`, `deploy`, `install`, `logs` 실행
- 플랫폼 문서 조사와 정리
- 문서 초안

핵심 로직인 ADF 평문 변환은 코드를 쓰기 전에 규칙을 먼저 정했다 (`DECISIONS.md` 1.7).
규칙은 데모 사이트에서 직접 측정한 응답에서 도출했고, 에이전트는 그 규칙대로 구현한다.

## 3. 슬라이스별 기록

### 슬라이스 0. Forge 스캐폴드 (`00d2269`)

**지시**: Forge 앱 스캐폴드 생성과 레포 배치.

**산출**: `forge create` 로 `confluence-content-action-ui-kit` 템플릿 생성. 템플릿이 만든
하위 디렉토리를 레포 루트로 올리고 무시 규칙 병합.

**판단**: 에이전트가 템플릿이 만든 하위 디렉토리를 그대로 두는 안과 루트로 올리는 안을 냈다.
관행에 어긋나는 배치가 아닌지 먼저 확인시켰고, 단일 앱 레포는 `manifest.yml` 을 루트에
두는 것이 일반적이었다. 앱이 하나뿐인 레포에서 forge 명령을 매번 한 단계 들어가서 칠 이유가
없어 루트로 올렸다.

**검증**: `forge lint` 통과. development 환경에 배포하고 데모 사이트 Confluence 에 설치해
왕복 확인.

**직접 확인**: 배포와 설치 명령의 성공 출력까지만 봤다. 이 시점의 화면은 템플릿의
hello world 라 Confluence 에서 열어보지 않았다. 화면을 실제로 연 것은 슬라이스 2 다.

### 슬라이스 1. 에이전트 지침 (`279cfc4`)

**지시**: 이전 프로젝트의 `CLAUDE.md` 를 참고해 커밋 규칙을 포함한 프로젝트 지침 정리.
문서 세 개가 요구 항목 여덟 개를 빠짐없이 담도록 역할 배정.

**산출**: Forge 플랫폼 제약, 명령어, 완료 기준, 작업 방식, 범위, 코드 규약, 커밋 규약,
문서 역할 분담.

**판단**: 템플릿이 `AGENTS.md` 를 함께 넣어줬다. Claude 를 쓰는데 다른 도구용 파일이 왜
생겼는지 물었고 (4.2), 내용의 절반은 값어치가 있고 절반은 이 레포에 남길 성격이 아니라
추려서 `CLAUDE.md` 로 옮기고 원본은 지웠다.

건진 것은 UI Kit 컴포넌트 제한, Storage 접근 경로, 스코프 변경 시 재설치 필요,
`--non-interactive` 플래그. 버린 것은 영어 장문 주석 지시와 말장난 지시.

**검증**: 금지 어휘와 em dash 검사 통과. 199줄로 권장 상한 이내.

**직접 확인**: `CLAUDE.md` 전문을 읽고 직접 수정했다. 이전 프로젝트의 지침을 그대로
복제하지 않도록 문서 역할 분담을 이 프로젝트에 맞게 다시 배정했다.

### 슬라이스 2. 리스크 실측

**지시**: 설계를 확정하기 전에 플랫폼이 실제로 무엇을 주는지 측정. 측정 코드는 커밋하지
않고 결과만 문서로 남긴다.

**방식**: 에이전트가 리졸버에 임시 측정 코드를 넣고 배포하면, Confluence 페이지에서 앱을
열어 실행하고 `forge logs` 로 결과를 수집했다. 데모 사이트의 시드 회의록은 액션 아이템이
전부 비어 있어서, 평문, 멘션, 링크, 서식, 긴 글자 다섯 종류를 넣은 페이지를 직접 만들어
측정했다.

**측정한 것과 결과**:

| 확인한 것 | 결과 |
|---|---|
| tasks API 의 `page-id` 필터와 ADF body | 동작. 단 `value` 가 객체가 아니라 JSON 문자열 |
| 태스크 객체 필드 | `id`, `localId`, `status`, `pageId`, `spaceId`, `assignedTo`, `body`, `dueAt` |
| `localId` 를 매핑 키로 쓸 수 있는지 | 불가. 시드는 `1`, 신규는 UUID 로 형식이 일정하지 않음 |
| 멘션과 담당자 연결 | 액션 아이템의 멘션이 `assignedTo` 에 accountId 로 채워짐 |
| 서식이 들어간 한 줄의 구조 | 조각 4개로 분리. 이어붙이지 않으면 제목이 잘림 |
| 빈 체크박스의 내용 | `placeholder` 조각. 거르지 않으면 안내 문구가 이슈 제목이 됨 |
| 앱 자격으로 태스크를 읽을 수 있는지 | 읽힘. 단 사이트 전체로는 사용자 61건 대 앱 5건 |

측정 없이 문서만 보고 설계했다면 최소 세 가지를 틀렸을 것이다. JSON 이중 인코딩, 조각 분리,
멘션 조각에 `text` 필드가 없다는 점이다.

**검증**: 측정 결과를 `DECISIONS.md` 에 옮기고 실측 표시. 측정 코드 원복 후 `forge lint`
통과와 워킹 트리 정리 확인.

**직접 확인**: Confluence 에서 앱을 세 번 실행했다. 첫 실행에서 시드 회의록의 액션 아이템이
전부 비어 있는 것을 화면에서 확인했고, 그래서 다섯 종류를 넣은 페이지를 직접 만들었다.
두 번째 실행에서 측정값이 그대로인 것을 보고 페이지를 편집만 하고 게시하지 않았다는 것을
찾아냈다. 게시 후 세 번째 실행에서 멘션과 서식이 반영된 응답을 얻었다.

### 슬라이스 3. ADF 평문 변환 스펙

**지시**: 구현 전에 변환 함수의 동작을 테스트로 고정. 함수 경계와 멘션 표기를 먼저 정하고
시작.

**판단**: 에이전트가 함수를 둘로 나누는 안과 하나로 합치는 안, 멘션의 `@` 를 남기는 안과
떼는 안을 냈다. 둘로 나누고 `@` 는 남기기로 했다. 근거는 `DECISIONS.md` 1.10.

**산출**: `toPlainText` 12건, `titleFromTask` 11건. 슬라이스 2 에서 측정한 응답 6건을
fixture 로 사용. Vitest 설치와 `npm test` 스크립트. 구현 파일은 만들지 않았다.

fixture 에 넣어둔 긴 항목(id 200)을 쓰는 케이스가 처음에 빠져 있었다. 제한을 넘는 경우만
있고 길지만 제한 이내인 경우가 없어서 추가했다.

**검증**: `npm test` 가 두 파일 모두 구현 모듈 미존재로 실패하는 것을 확인. 의도한 red
상태다. 빈 스텁을 두지 않은 이유는 `null` 을 기대하는 테스트가 우연히 통과할 수 있어서다.
`forge lint` 는 오류 없음. 배포본(3.2.0)에 실측용 스코프가 남아 있어 메이저 버전 승인 항목이
뜨는데, 슬라이스 5 에서 실제 스코프를 넣고 배포하면 정리된다.

**직접 확인**: `npm test` 를 직접 실행해 red 상태를 확인했다. 두 파일이 `Cannot find module`
로 실패하고 `Tests no tests` 가 뜬다. 테스트가 틀려서 실패한 것이 아니라 구현이 없어 수집
단계에서 멈춘 것이다. 슬라이스 4 에서 같은 명령이 23건 통과로 바뀌는지가 다음 확인 대상이다.

### 슬라이스 4. ADF 평문 변환 구현

**지시**: 슬라이스 3 의 테스트를 통과시킨다. 구현 전에 설계부터.

**판단**: 설계 단계에서 에이전트가 조각을 이어붙이는 방식에 함정이 있다고 보고했다.
문단 두 개를 붙이려면 구분자가 필요한데, 인라인 조각 사이에 같은 구분자를 넣으면
단어 가운데가 갈라진다. 서식이 단어 중간에 걸린 경우다.

기존 테스트 23건은 이 경우를 잡지 못했다. 규칙만 올바르게 구현하고 넘어가는 대신
테스트를 1건 추가해서 막기로 했다. 나중에 누가 규칙을 되돌려도 테스트가 잡는다.

근거는 실측에 있다. 측정한 조각이 `'굵게' | ' 와 ' | '기울임' | ' 섞기'` 였고 공백이 text
조각 안에 이미 들어 있었다. 그러므로 인라인끼리는 구분자를 넣지 않고, 블록 사이에만
공백 하나를 넣는다.

**산출**: `src/lib/adf-text.js` 의 `toPlainText`, `src/lib/task-title.js` 의 `titleFromTask` 와
`TITLE_MAX_LENGTH`. 테스트 24건 통과.

**검증**: `npm test` 24건 통과, `forge lint` 오류 0.

첫 실행에서 5건이 실패했다. 태스크 `id` 가 정수인 줄 알고 테스트를 짰는데 API 는
`"196"` 처럼 문자열로 준다. `DECISIONS.md` 1.5 에 "정수다" 라고 적어둔 것도 같이 틀렸다.
테스트를 문자열 비교로 고치고 문서를 정정했다 (4.5).

**직접 확인**: `npm test` 를 직접 실행해 24건 통과를 확인했다. 슬라이스 3 에서 본
`Tests no tests` 가 `Tests 24 passed` 로 바뀌었다. 구현 파일 두 개도 읽었다.

배포는 하지 않았다. 이 슬라이스는 Forge 코드와 manifest 를 건드리지 않는다. 테스트 파일이
배포본에 딸려가지 않는지는 슬라이스 5 에서 리졸버가 이 함수를 import 할 때 확인한다.

### 슬라이스 5. 태스크 조회와 목록 화면

**지시**: 리졸버가 무엇인지 설명부터 하고 설계. 그 다음 구현.

**판단**: 에이전트가 목록에서 제목을 바로 고칠 수 있게 하는 안을 냈다. 조회가 제대로 도는지
먼저 확인하고 편집은 이슈 생성과 같은 슬라이스로 미뤘다. 조회 문제와 편집 문제가 섞이면
어느 쪽이 깨졌는지 가리기 어렵다.

모듈 키에 `hello-world` 가 남아 있던 것도 이번에 정리했다. 스코프 변경으로 어차피
재설치하므로 추가 비용이 없는 시점이었다.

**산출**: `getTasks` 리졸버, UI Kit 목록 화면, manifest 스코프 2개와 모듈 키 정리.
리졸버 계약과 화면 상태 결정은 `DECISIONS.md` 1.11.

**검증**: `npm test` 24건 통과로 회귀 없음. `forge lint` 오류 0. 3.3.0 배포와 재설치.

첫 배포가 번들링에서 실패했다. `forge lint` 는 통과했는데 `@forge/api` 를 찾지 못했다.
슬라이스 2 의 실측 코드를 원복할 때 함께 지운 의존성을 이번 리졸버가 다시 쓴다.
다시 설치해서 해결했다 (4.7).

슬라이스 4 에서 미뤄둔 확인도 끝났다. `src/lib` 의 테스트 파일이 배포본에 들어갔다면
`vitest` import 때문에 리졸버 로딩이 깨진다. 화면이 정상 동작하므로 번들러가 핸들러에서
import 되는 파일만 묶는다는 것이 확인됐다.

**직접 확인**: 두 페이지에서 화면을 열어 확인했다. 직접 만든 회의록에서는 5건이 제목으로
떴고 체크박스 선택에 따라 개수가 따라 바뀌었다. 시드 회의록에서는 `빈 항목 2건 제외` 와
함께 안내 문구가 떴다. 변환 규칙이 실제 데이터에서 도는 것을 화면으로 본 셈이다.

### 슬라이스 6. Jira 연결과 대상 선택

**지시**: 슬라이스 6 을 시작하기 전에 구현 방식을 설계로 먼저 정리하게 했다.

**판단**: 에이전트가 원래 하나로 잡았던 범위에 위험이 셋 섞여 있다고 보고했다. 제품이
늘어나는 것, 다른 제품을 읽는 것, 처음으로 쓰기를 하는 것이다. 읽기까지를 슬라이스 6,
쓰기를 7 로 나눴다. 화면이 안 뜰 때 원인을 가릴 수 있어야 한다.

이슈 타입을 사용자에게 고르게 할지도 정했다. 회의록의 액션 아이템은 성격이 전부 할 일이라
선택지를 노출하는 대가로 얻는 것이 없다고 보고 자동 선택으로 갔다. 대신 무엇이 정해졌는지
화면에 표시하기로 했다 (`DECISIONS.md` 1.12).

**산출**: `getProjects`, `getIssueTypes` 리졸버. 프로젝트 선택과 제목 수정 화면.
`app.compatibility` 선언과 `read:jira-work` 스코프.

**검증**: `npm test` 24건 통과로 회귀 없음. 4.0.0 배포 후 Confluence 재설치와 Jira 신규 설치.
`forge lint` 가 `compatibility` 오류를 배포 전에 잡았다 (4.8).

**직접 확인**: 화면을 열어 네 가지를 확인하고 두 가지 문제를 지적했다.

동작한 것은 프로젝트 드롭다운에 시드 프로젝트가 뜨는 것, 제목 입력창 수정, 체크를 풀면
그 줄이 비활성화되는 것, 255자를 넘겼을 때 현재 글자 수와 함께 오류가 뜨는 것이다.

지적한 것 첫 번째는 **이슈 타입에 `Task` 가 아니라 `작업` 이 떴다**는 점이다. 화면상으로는
맞는 값이 나왔지만 코드는 `Task` 를 찾게 되어 있었다. 이름이 번역돼서 온다는 것을 에이전트가
몰랐고, 매칭 실패로 목록의 첫 번째가 우연히 걸린 상태였다 (4.9).

두 번째는 **프로젝트를 고르기 전에는 그 자리가 비어 있다가 갑자기 문구가 생겨** 목록이
아래로 밀린다는 점이다. 선택 전에도 안내 문구로 자리를 채우고 `이슈 타입: 작업` 형태로
바꿨다.

목록이 프로젝트 선택 전에 이미 렌더링되는 것도 물었다. 읽기는 되돌릴 수 있고 쓰기만
되돌릴 수 없으므로 잠금을 목록이 아니라 생성 버튼에 걸기로 정리했다 (`DECISIONS.md` 1.11).

### 슬라이스 7. 이슈 생성과 매핑 저장

**지시**: 구현 방식을 설계로 먼저 정리하게 했다.

**판단**: 이번 슬라이스에서 처음으로 되돌릴 수 없는 일을 한다. 설계의 무게를 무엇을
만드는가가 아니라 잘못됐을 때 어떻게 되는가에 두기로 하고 세 가지를 정했다.

**부분 실패**는 전부 시도하고 결과를 항목별로 보고하는 쪽으로 갔다. 에이전트가 전부 아니면
전무, 첫 실패에서 중단, 전부 시도의 세 안을 냈다. 이슈 생성 실패의 원인은 항목마다 다르고,
한 건의 실패가 나머지가 잘못됐다는 뜻이 아니다. 되돌리는 방식은 이슈 삭제 권한을 추가로
요구하게 되는데 그 권한을 요구하면 관리자가 설치를 꺼린다.

**담당자 실패**는 담당자를 빼고 재시도하기로 했다. 사용자가 원하는 것은 할 일이 Jira 에
있는 것이지 담당자가 정확히 붙는 것이 아니다. 이슈만 있으면 담당자는 Jira 에서 한 번에
고치지만, 이슈가 없으면 회의록으로 돌아가 다시 옮겨야 한다.

**중복 표시**는 이번에 넣기로 했다. 원래 개선 방향에 있던 항목인데, 모달을 두 번 열면 이슈가
두 개 생기는 실제 결함이고, 매핑이 저장됐는지 확인할 유일한 수단이기도 하다.

근거는 `DECISIONS.md` 1.13 부터 1.15 에 있다.

**산출**: `createIssues` 리졸버, `src/lib/mapping.js`, 결과 화면, 생성 버튼 잠금.
`write:jira-work` 와 `storage:app` 스코프.

**검증**: `npm test` 24건 통과로 회귀 없음. 5.0.0 배포 후 두 제품 재설치.

`forge lint` 가 경고를 냈다. `@forge/api` 의 `storage` 가 폐기 예정이고 `@forge/kvs` 를
쓰라는 내용이다. 인터페이스가 같아서 import 만 바꿔 옮겼다. 경고 단계에서 옮기는 편이
나중에 깨지는 것보다 낫다.

**직접 확인**: 네 단계를 순서대로 확인했다.

프로젝트를 고르기 전에는 버튼이 잠겨 있고 잠긴 이유가 표시된다. 체크를 전부 풀면 이유가
바뀐다. 1건만 선택해 만들면 결과 화면에 이슈 키가 뜬다. Jira 로 건너가 이슈가 실제로
있는 것을 확인했다.

마지막으로 모달을 닫고 다시 열어 **방금 만든 항목이 선택 해제 상태로 이슈 키와 함께
뜨는 것**을 확인했다. Forge Storage 는 눈으로 볼 수 없어서 이것이 매핑 저장을 확인하는
유일한 방법이다. 여기가 안 되면 역방향이 통째로 성립하지 않는다.

이 슬라이스에서 계획 단계의 리스크 두 개가 함께 해소됐다. `asUser` 로 Jira 이슈를 만들 수
있는지, `assignedTo` 의 accountId 가 Jira 에서 통하는지다.

### 슬라이스 7-1. 이슈 본문에 회의록 링크

**지시**: 슬라이스 7 의 결과를 확인하다가 만들어진 이슈에 제목만 있고 본문이 비어 있는 것을
발견해 의도한 것인지 물었다.

**판단**: 의도한 것이 아니라 빠뜨린 것이었다. 설계 문서 어디에도 본문에 대한 결정이 없었다.

그리고 단순한 필드 누락이 아니라 주제와 직결되는 구멍이었다. 매핑은 Forge Storage 안에만
있어서 사람 눈에 보이지 않는다. Jira 에서 이슈를 본 사람이 원본 회의록으로 건너갈 방법이
없었다. 두 곳을 잇는 앱인데 정작 사람이 건널 다리가 없는 셈이다.

슬라이스 7 은 이미 커밋했으므로 별도 커밋으로 채우기로 했다. 관심사가 다르고, 지적을 받고
채운 과정이 히스토리에 남는 편이 낫다.

**산출**: `src/lib/issue-description.js` 와 테스트 5건. 리졸버가 페이지를 한 번 읽어 제목과
주소를 가져오고 ADF 본문을 만들어 붙인다.

여기서 ADF 를 처음으로 쓴다. 슬라이스 4 에서 읽던 형식을 이번에는 만들어서 보낸다.
Jira v3 API 의 `description` 은 평문이 아니라 ADF 를 받는다.

테스트를 붙인 이유는 화면으로 확인할 수 없는 종류이기 때문이다. ADF 모양이 틀리면 Jira 가
400 으로 거절하는데, 그러면 본문만 빠지는 것이 아니라 이슈 생성 자체가 실패한다. 결과
화면에는 `생성 실패` 로만 뜨고 원인이 보이지 않는다.

**검증**: `npm test` 29건 통과. `forge lint` 오류 0. 5.1.0 배포.

**직접 확인**: 새 항목을 하나 만들어 Jira 에서 본문을 확인했다. 안내 문장과 회의록 링크가
들어갔고, 링크를 눌러 원본 회의록으로 돌아가는 것까지 확인했다. 개인 스페이스라 주소에
`~712020...` 형태의 긴 키가 들어가는데 그대로 동작한다.

슬라이스 7 에서 미뤄둔 담당자 확인도 함께 했다. 회의록에서 멘션한 사람이 Jira 이슈 담당자로
그대로 들어간다. `DECISIONS.md` 1.6 의 accountId 승계 설계가 실측으로 검증됐다.

### 슬라이스 8. 역방향 상태 반영

**지시**: 역방향이 무엇인지 설명을 먼저 듣고, 설계를 잡은 뒤 구현.

**판단**: 완료 판별을 무엇으로 할지가 핵심이었다. 에이전트가 상태 이름, `jiraEventTypeName`,
`statusCategory` 세 안을 냈다. 상태 이름은 프로젝트마다 언어마다 다르므로 슬라이스 6 의 이슈
타입 번역 함정과 같은 종류다. `statusCategory` 로 정했다.

되돌리기도 반영하기로 했다. 완료만 따라가면 다리가 다시 한 방향이 되고, 같은 코드에서 값만
달라져 추가 비용이 없다.

manifest 의 `filter.expression` 은 이번에 쓰지 않기로 했다. 페이로드 모양을 모르는 상태에서
필터를 잘못 걸면 이벤트가 아예 안 오는데, 그러면 필터 문제인지 트리거 문제인지 구분이 안
된다. 코드에서 거르고 로그로 확인한 뒤 붙이는 것이 순서다.

**산출**: `src/reverse.js` 트리거 핸들러, manifest 의 `trigger` 모듈과 `write:task:confluence`.
근거는 `DECISIONS.md` 1.17 과 1.18.

**검증**: `npm test` 29건 통과로 회귀 없음. `forge lint` 오류 0. 6.0.0 배포 후 두 제품 재설치.

설계 단계에서 이벤트 이름을 확인했더니 계획서에 적어둔 `avi:jira:issue:updated` 가 틀렸다.
실제로는 `avi:jira:updated:issue` 로 동사가 앞에 온다. `DECISIONS.md` 1.1 다이어그램에도
같은 오류가 옮겨져 있어 함께 고쳤다 (4.11).

**직접 확인**: Jira 에서 이슈를 완료로 옮기고 회의록을 새로고침해 체크박스가 자동으로
체크되는 것을 확인했다. 다시 진행 중으로 되돌리니 체크가 풀렸다. 아무도 회의록을 건드리지
않았는데 두 곳의 상태가 맞춰졌다.

로그가 왕복을 그대로 보여준다.

```
[reverse] event PLAT-33510 changed=["resolution","status"]
[reverse] category PLAT-33510 done via payload
[reverse] task updated 201 complete

[reverse] event PLAT-33510 changed=["resolution","status"]
[reverse] category PLAT-33510 indeterminate via payload
[reverse] task updated 201 incomplete
```

이 로그로 세 가지가 확인됐다.

`statusCategory` 가 페이로드에 함께 온다 (`via payload`). 없을 때를 대비해 이슈를 다시
읽는 경로를 넣어뒀는데 실제로는 타지 않는다.

**`asApp` 이 통과했다.** 슬라이스 2 측정에서 앱 자격으로 사이트 전체 61건 중 5건만 보였기
때문에 역방향이 성립하지 않을 위험이 가장 컸다. 데모 사이트에서 만든 페이지로는 상태 변경이
된다. 다만 보이는 범위 문제 자체는 남아 있어 한계로 둔다.

완료로 옮길 때 `changed` 에 `resolution` 이 함께 온다. 상태와 해결책이 같이 바뀌는 것인데,
`status` 만 보는 판별이 맞았다는 뜻이기도 하다.

전달 시간도 확인했다. 문서상 최대 3분인데 실측에서는 20초 안에 도착했다.

### 슬라이스 9. 회의록 쪽 연결 표시

**지시**: 기능을 더 붙이고 싶다고 하고 후보를 받았다. 문서에 적어둔 한계와 개선 방향에서
뽑은 다섯 가지 중 회의록 쪽에 연결을 보여주는 것을 먼저 골랐다. 설계부터 정리하게 했다.

**판단**: 슬라이스 7-1 로 Jira 에서 회의록으로 가는 링크는 생겼지만 반대가 없었다. 회의록을
읽는 사람은 더보기 메뉴를 열지 않아 이 앱이 있는지도 모른다. `contentBylineItem` 모듈로
제목 아래에 표시하기로 했다.

비용 구조가 지금까지와 다르다는 점을 설계에서 먼저 짚었다. `contentAction` 은 누를 때만
돌지만 바일라인은 페이지를 열 때마다 돈다. 라벨 계산에서 ADF 변환을 빼고 개수만 세도록
경로를 나눴다 (`DECISIONS.md` 1.20).

**산출**: `src/byline.js` 의 `bylineProperties`, `getPageLinks` 리졸버, `src/frontend/byline.jsx`
팝업. 제품 호출 헬퍼를 `src/lib/product-api.js` 로 공용화. 리졸버와 바일라인이 진입점은
다르지만 오류 처리는 같아야 한다.

**검증**: `npm test` 29건 통과로 회귀 없음. `forge lint` 오류 0. 6.1.0 에서 6.3.0 까지
세 번 배포하며 화면을 보고 고쳤다.

**직접 확인**: 네 종류의 페이지를 열어 확인했다.

연결이 있는 회의록에서 `이슈 2건 연결됨` 이 뜨고, 팝업에 이슈 목록과 상태, Jira 링크가
나왔다. 완료로 표시된 항목은 회의록 체크박스도 체크돼 있어 역방향의 결과가 같은 화면에서
확인됐다.

**연결이 없는 페이지에서 항목이 숨겨지지 않는 것을 발견해 지적했다.** 빈 `title` 을
돌려줘도 manifest 기본값인 `Task Bridge (Development)` 가 그대로 나온다 (4.12).

빈 상태 안내가 부정확한 것도 지적했다. 내용이 빈 액션 아이템만 있는 페이지에서 "더보기
메뉴에서 열면 옮길 수 있습니다" 라고 안내해 헛걸음을 시켰다. 팝업 경로는 `body-format` 을
붙여 조회하므로 내용 유무를 판별할 수 있어, 세 가지 안내로 나눴다.

배포 직후 팝업 문구가 이전 버전으로 보이는 것도 확인했다. 라벨은 서버에서 계산해 내려오고
팝업은 브라우저가 받은 코드로 그려서 갱신 시점이 다르다. 강제 새로고침으로 해결됐다.

### 슬라이스 10. 역방향 실패를 이슈 댓글로 알림

**지시**: 후보 중 두 번째로 이것을 골랐다. 설계부터 하게 했다.

**판단**: 에이전트가 정리한 설계에서 실패 지점 여섯 개를 표로 받고, 어디에 댓글을 달지를
두 갈래로 좁혀 골랐다.

첫째는 범위다. **4xx 만** 알리기로 했다. 5xx 와 네트워크 예외는 다음 상태 변경 때 재시도되고
사람이 할 일이 없다. 할 일이 없는 댓글은 소음이고, 소음이 쌓이면 정작 볼 것을 안 보게 된다.
대신 지속적인 5xx 가 안 보이게 되는 것은 한계로 받아들이고 문서에 남겼다.

둘째는 중복 방지다. 이슈의 기존 댓글을 조회해 판정하는 안 대신 **Storage 플래그**를 골랐다.
매핑 레코드를 어차피 읽고 있어 추가 호출이 없고, 본문 문자열 매칭에 기대지 않는다.
반영이 다시 성공하면 플래그를 지우게 해서, 값의 뜻이 "예전에 알린 적 있다" 가 아니라
"지금 끊겨 있고 이미 알렸다" 가 되도록 했다.

설계 단계에서 에이전트가 자기가 앞서 적어둔 개선 항목을 뒤집은 것도 그대로 받았다 (4.13).

**산출**: `src/lib/failure-comment.js` (순수 함수), `src/lib/adf-build.js` (이슈 본문과
댓글이 함께 쓰는 ADF 헬퍼), `src/lib/mapping.js` 의 `markNotified` 와 `clearNotified`,
`src/reverse.js` 의 `notifyFailure`. 배포 확인에서 댓글에 회의록 링크가 안 붙는 것을
발견해 매핑에 `pageUrl` 을 저장하는 쪽으로 고쳤다 (4.14). manifest 는 건드리지 않았다. 댓글 생성이 이미 가진
`write:jira-work` 에 포함돼 스코프를 늘리지 않아도 됐고, 그래서 재설치도 필요 없었다.

**검증**: 실패하는 테스트를 먼저 쓰고 red 를 확인한 뒤 구현했다. `npm test` 36건 통과
(신규 7건), `forge lint` 오류 0. 6.4.0 과 6.5.0 두 번 배포했다.

**직접 확인**: 버릴 페이지를 만들어 실패를 일부러 만들고, 네 갈래가 실제로 갈리는지 봤다.
회의록에서 액션 아이템 줄을 지우면 태스크 id 가 사라져 반영이 404 로 거부된다.

| 상황 | 화면 | 로그 |
|---|---|---|
| 줄을 지운 뒤 완료로 이동 | 이슈에 댓글 1건 | `notify ... 404 pageUrl yes` |
| 진행으로 되돌렸다 다시 완료 | 댓글 그대로 1건 | `already notified` 두 번 |
| 연결이 살아 있는 이슈를 완료로 | 회의록 체크, 댓글 없음 | `task updated 236 complete` |
| 손으로 만든 남의 이슈를 완료로 | 아무 일 없음 | `no mapping` |

두 번째 줄이 이 슬라이스의 확인 지점이었다. 되돌릴 때도 태스크가 없어 404 가 나는데
거기서도 댓글이 안 붙었다. 설계에서 상태 방향을 따로 따지지 않았는데 플래그가 방향과
무관해서 자연히 막혔다.

댓글의 회의록 링크를 눌러 해당 페이지로 이동하는 것까지 확인했다. 6.4.0 때 만든 매핑은
`pageUrl no`, 6.5.0 이후 매핑은 `yes` 로 로그에서 갈렸다. 이전 매핑에 링크가 없는 것은
고치지 않고 한계로 두었다.

**확인하지 않은 것**: 403 으로 거부되는 경우다. 앱이 편집할 수 없는 공간을 만들어야 하는데
데모 사이트에서 재현하지 않았다. 404 와 같은 분기를 타고 문구만 다르다. 5xx 도 만들지
못했고, 애초에 알리지 않기로 한 경우다.

## 4. 에이전트가 틀렸던 사례

에이전트 산출물을 그대로 받지 않고 확인해야 하는 이유다. 어떻게 드러났는지를 같이 적는다.

### 4.1 문서 조사 기반 계획이 플랫폼 변경을 놓쳤다

작업 시작 전에 문서 조사로 세운 계획에 Developer Space 가입 단계가 없었다. Forge 는 이제
모든 신규 앱을 Developer Space 안에 만들어야 하는데, `forge create` 를 실제로 실행하면서
드러났다.

같은 계획에 개발 사이트를 웹에서 수동 생성한다고 적혀 있었으나, `forge site provision` 이
생겨서 시드 데이터가 포함된 사이트를 명령 한 번으로 받을 수 있었다.

계획을 실측 결과로 교정했다. 이후 설계 결정은 문서 근거와 실측 근거를 구분해서
`DECISIONS.md` 에 표시한다.

### 4.2 에이전트가 자기 동작에 대해 틀린 주장을 했다

템플릿이 넣어준 `AGENTS.md` 를 두고 에이전트가 "그대로 두면 영어 장문 주석을 달게 된다" 고
경고했다. Claude 를 쓰는데 왜 다른 도구용 파일이 영향을 주는지 물었다.

에이전트가 공식 문서를 확인한 결과 Claude Code 는 `AGENTS.md` 를 읽지 않고 `CLAUDE.md` 만
읽는다. 경고 자체가 틀렸다. 지침은 `CLAUDE.md` 로 옮겼다.

### 4.3 승인 없이 범위를 벗어났다

실측 단계에서 에이전트가 승인 없이 `manifest.yml` 을 스코프 6개짜리로 교체하고, 임시
웹트리거 모듈과 의존성을 추가했다. 모듈 키가 중복되어 `forge lint` 가 실패한 시점에 멈춰
세웠다. 슬라이스 단위로 나누어 커밋하며 진행하고, 결정과 지시에 근거해서만 작업하도록
방식을 바꿨다.

전부 원복시키고, 슬라이스 단위 승인과 커밋 시점을 `CLAUDE.md` 에 규칙으로 넣어
커밋(`279cfc4`)으로 고정했다.

```
- 슬라이스 하나 = 커밋 하나
- 슬라이스를 시작하기 전에 범위와 방식을 제안하고 승인을 받는다. 코드부터 쓰지 않는다
- 커밋은 사람의 지시로 진행한다. 알아서 커밋하지 않는다
```

### 4.4 비대화형 환경에서 대화형 명령을 반복 시도했다

에이전트의 셸에 TTY 가 없어 `forge login` 과 `forge create` 의 프롬프트가 뜨지 않았다.
`expect` 로 가짜 터미널을 붙여 우회하려다 두 번 연속 실패했다.

두세 번 실패하면 멈추고 보고하는 규칙에 따라 에이전트가 중단하고 넘겼다. 로그인은 환경변수
파일로, 앱 생성은 터미널에서 직접 실행했다. 이후 `--non-interactive` 플래그로 대부분의
명령이 해결된다는 것을 확인했다.

### 4.5 태스크 id 를 정수로 단정했다

슬라이스 2 의 측정 결과를 문서로 옮기면서 에이전트가 `DECISIONS.md` 1.5 에 "`id` 는 사이트
전역에서 고유한 정수다" 라고 적었다. API 는 `"196"` 처럼 문자열로 준다. 측정 데이터를 화면에
찍을 때 따옴표 없이 출력해서 타입을 확인하지 않고 넘어간 것이다.

슬라이스 4 에서 테스트가 잡았다. fixture 를 `id === 200` 으로 찾는 케이스 5건이 전부 `null` 을
받았다. 구현이 아니라 문서의 사실 오류였고, 테스트가 없었으면 매핑 키를 정수로 다루다가
역방향 조회에서 터졌을 것이다.

문서를 정정하고, 테스트의 fixture 조회에 못 찾으면 던지는 가드를 넣었다. 조용히 `undefined`
가 흘러 `null` 이 되는 대신 원인이 바로 보이게 했다.

### 4.6 진행 중인 문서를 완성본처럼 썼다

`DECISIONS.md` 초안에 아직 시작하지 않은 기능이 구현 범위로 적혀 있었고, 이 문서는
"개발했다" 로 끝나 있었다. 커밋 전에 지적해서 구현 현황 표로 바꾸고 미착수를 미착수로
표시했다.

### 4.7 원복하면서 필요한 의존성까지 지웠다

슬라이스 2 의 실측 코드를 걷어낼 때 에이전트가 `@forge/api` 를 함께 제거했다. 실측용으로
설치한 것이 맞지만, 리졸버가 제품 API 를 부르려면 계속 필요한 패키지였다.

슬라이스 5 의 첫 배포가 번들링에서 멈추면서 드러났다. 주목할 점은 `forge lint` 가 그 직전에
통과했다는 것이다. 린트는 manifest 문법과 알려진 오류만 보고 import 해석까지는 하지 않는다.
배포까지 가야 잡히는 종류의 오류가 있다는 뜻이다.

`CLAUDE.md` 완료 기준의 "린트만으로는 부족하다" 에 근거가 하나 더 붙었다.

### 4.8 제품 호환 선언을 잘못 읽었다

Atlassian 문서의 "required 로 지정할 수 있는 것은 Jira 또는 Confluence 중 하나뿐이다" 를
에이전트가 "하나만 적으면 된다" 로 읽고 `jira: required: false` 만 선언했다. 실제로는 정확히
하나가 `required: true` 여야 한다는 뜻이었다.

`forge lint` 가 배포 전에 잡았다.

```
One context (Jira, Confluence, or Compass) must have `required:true` in app.compatibility.
```

문서를 읽고 세운 가정이 또 틀렸지만 이번에는 도구가 먼저 잡았다. 4.7 에서 린트가 못 잡는
종류를 봤다면 이번은 잡는 종류였다. 무엇을 어느 단계에서 거를 수 있는지가 조금씩 드러난다.

### 4.9 이슈 타입 이름이 번역된다는 것을 놓쳤다

에이전트가 이슈 타입을 이름으로 골랐다.

```js
types.find((t) => t.name === 'Task') ?? types[0]
```

화면에서 `이슈 타입 작업` 이 뜬 것을 보고 `Task` 가 아닌데 왜 통과했는지 물었다.
확인해보니 이슈 타입 이름은 보는 사람의 언어로 번역돼서 온다. 한국어 사이트에서 `Task` 는
`작업` 이고, 매칭이 실패해 목록의 첫 번째로 떨어진 것이었다.

**화면상으로는 맞는 값이 나왔기 때문에 결과만 봐서는 잡을 수 없는 결함이다.** 그 프로젝트에서
목록 첫 번째가 우연히 `작업` 이었을 뿐이다. 응답 원본을 로그로 확인해서 드러났다.

```
id=10002  name='작업'       untranslatedName='Task'         hierarchyLevel=0
id=10004  name='새 기능'     untranslatedName='New Feature'  hierarchyLevel=0
id=10006  name='[System] Incident'                          hierarchyLevel=0
```

시드 데이터에는 `[System] Incident` 나 `Access` 같은 타입이 섞여 있다. 순서가 다른
프로젝트였다면 회의록의 할 일이 장애 보고서로 만들어졌을 것이다.

`untranslatedName` 을 먼저 보고, 그 다음 `name`, 그 다음 `hierarchyLevel === 0` 순으로
고치고 `DECISIONS.md` 1.12 에 근거와 함께 남겼다.

### 4.10 이슈 본문을 아예 설계하지 않았다

에이전트가 이슈를 만들면서 `summary` 만 넣고 `description` 을 넣지 않았다. 결함이 아니라
누락이다. 설계 문서에 본문에 대한 결정 자체가 없었고, 그래서 검토 단계에서도 걸리지 않았다.

만들어진 이슈를 Jira 에서 열어보고 본문이 비어 있는 것을 발견해 의도한 것인지 물었다.

앞의 사례들과 종류가 다르다. **틀린 것이 아니라 없는 것이라 어떤 도구도 잡을 수 없다.**
테스트는 있는 코드가 맞게 도는지를 보고, 린트는 문법을 보고, 화면은 그려진 것을 보여준다.
빠진 결정을 찾아내는 것은 사람이 결과물을 보고 "이게 맞나" 를 묻는 것뿐이다.

그리고 이 누락은 주제와 직결됐다. 매핑이 Forge Storage 안에만 있어서 Jira 쪽에서는 원본
회의록으로 건너갈 방법이 없었다. 두 곳을 잇는 것이 이 앱의 목적인데 사람이 건널 다리가
빠져 있었다.

`DECISIONS.md` 1.16 에 결정으로 추가하고 본문 링크를 구현했다.

### 4.11 계획서의 이벤트 이름이 틀렸고 설계 문서로 옮겨졌다

작업 시작 전 문서 조사로 세운 계획에 Jira 이벤트 이름이 `avi:jira:issue:updated` 로 적혀
있었다. 실제로는 `avi:jira:updated:issue` 로 동사가 앞에 온다.

문제는 이 값이 슬라이스 2 에서 `DECISIONS.md` 1.1 다이어그램으로 그대로 옮겨졌다는 점이다.
설계 문서에 다섯 슬라이스 동안 틀린 값이 남아 있었다. 아무도 그 사이에 확인하지 않았다.

슬라이스 8 설계 단계에서 공식 문서를 확인하다가 드러났다. 구현에 들어가기 전이라 손해는
없었지만, **틀린 값이 검증 없이 문서 사이를 옮겨 다닐 수 있다**는 것을 보여준다. 4.1 에서
"문서 근거와 실측 근거를 구분해 표시한다" 고 정했는데, 1.1 다이어그램에는 그 표시가 없었다.

`DECISIONS.md` 1.1 과 1.17 을 함께 고쳤다.

### 4.12 관측하려고 만든 코드에 조용히 끝나는 경로를 남겼다

바일라인 라벨이 연결 없는 페이지에서 숨겨지지 않는 것을 발견하고 원인을 찾으려 로그를
봤는데 아무것도 없었다. 그러면 두 가지가 구분되지 않는다. 핸들러가 아예 안 돈 것인지,
돌았는데 조용히 끝난 것인지다.

원인은 에이전트가 쓴 코드에 있었다. 태스크가 0건인 경로에만 로그가 빠져 있었다.

```js
const tasks = ...;
if (tasks.length === 0) return { title: '' };   // 로그 없이 종료
```

**하필 확인하려던 상황이 그 경로였다.** 로그가 유일한 관측 수단인 코드에서 조용히 끝나는
길을 남기면 관측 자체가 성립하지 않는다. 역방향(`src/reverse.js`)에서는 단계마다 로그를
남기도록 설계해놓고, 바일라인에서는 같은 원칙을 적용하지 않았다.

연결이 있는 페이지를 열어보는 것으로 배포 없이 판가름했다. 거기서 `이슈 2건 연결됨` 이
떴으므로 핸들러는 도는 것이고, 빈 `title` 이 무시되는 것이 원인이었다.

모든 경로에 로그를 넣고, 조건부 숨김이 불가능하다는 것을 `DECISIONS.md` 2.3 에 한계로
남겼다. 툴팁만 돌려주면 부분 적용된다는 것도 함께 확인해 대안으로 썼다.

### 4.13 자기가 적어둔 개선 항목이 그대로 하면 스팸이 되는 설계였다

슬라이스 8 을 마치면서 에이전트가 `DECISIONS.md` 3 에 개선 방향을 적었다.

> 이슈가 완료됐는데 매핑된 태스크를 못 찾으면 이슈에 댓글로 남기면 사용자가 알 수 있다.

문장만 보면 맞는 말이라 두 슬라이스 동안 그대로 남아 있었다. 이번에 구현하려고 설계를
시키자 에이전트가 이 항목을 뒤집었다.

트리거는 사이트의 **모든** 이슈 상태 변경에 깨어난다. 그중 이 앱이 만든 이슈는 극소수고
나머지는 전부 매핑 없음으로 끝난다. 적어둔 대로 구현했으면 사람이 손으로 만든 이슈를
완료로 옮길 때마다 앱이 댓글을 달았을 것이다. 게다가 "원래 남의 이슈" 와 "우리 이슈인데
매핑을 잃었다" 를 가를 단서가 없다. 키가 이슈 키인데 그 레코드가 없는 상황이기 때문이다.

**틀린 지점은 실패 조건이었다.** 매핑이 없는 것은 실패가 아니라 대부분 무관한 이슈다.
실제 실패는 매핑이 살아 있는데 반영 요청이 거부되는 것이다.

기록해두는 이유는 이것이 도구로 안 걸리는 종류라서다. `forge lint` 도 테스트도 통과했을
것이고, 데모 사이트에서 우리가 만든 이슈만 눌러보면 정상으로 보였을 것이다. 문제는 손으로
만든 다른 이슈를 옮겼을 때 드러난다. 코드를 쓰기 전에 설계를 말로 풀게 한 것이 잡았다.

문서에도 왜 뒤집었는지를 남겼다 (`DECISIONS.md` 1.21).

### 4.14 이미 아는 값을 페이로드에서 추측해 조립하려 했다

댓글에 회의록 링크를 넣으려는데 트리거 페이로드에 사이트 주소가 없다. 에이전트가 낸 답은
이슈의 `self` 필드에서 주소를 떼어 쓰는 것이었다.

```js
`${new URL(event.issue.self).origin}/wiki/pages/viewpage.action?pageId=${pageId}`
```

**페이로드에 그 필드가 있는지 문서에 예시가 없다는 것을 스스로 적어놓고도 그대로 갔다.**
배포해서 실패를 만들어보니 댓글에 링크가 없었고, 로그가 `pageUrl no` 였다.

정방향 리졸버는 이슈 본문에 넣을 회의록 링크를 이미 만들고 있었다 (1.16). 사이트 주소도
페이지 경로도 그 자리에는 다 있다. 그 값을 매핑에 같이 저장하고 트리거는 읽기만 하면
추측이 필요 없다. 주소 품질도 더 낫다. 리졸버 쪽은 `_links.webui` 로 현재 주소를 만들고,
에이전트가 쓰려던 것은 pageId 로 여는 옛 리다이렉트 주소였다.

**틀린 지점은 값을 어디서 구할지였다.** 없는 곳에서 조립하려 하기 전에 아는 곳이 있는지
봤어야 했다. 같은 코드베이스 안에 답이 있었다.

로그에 `pageUrl yes|no` 를 남겨둔 덕분에 배포 한 번으로 갈렸다. 4.12 에서 조용히 끝나는
경로를 만들었다가 관측이 안 됐던 것과 반대 경우다.

## 5. 검증 장치

에이전트 산출물을 믿지 않고 확인하는 데 쓰는 것들이다. 아직 쓰지 않은 것은 언제부터
적용하는지 함께 적는다.

- **`forge lint`**: manifest 문법과 알려진 오류. 실제로 모듈 키 중복(4.3)을 여기서 잡았다
- **실제 배포와 설치**: 린트는 UI Kit 컴포넌트를 잘못 써서 렌더가 안 되는 것을 잡지 못한다.
  화면과 트리거를 건드린 작업은 데모 사이트에 배포해서 눈으로 확인한다
- **`forge logs`**: 배포된 앱의 실제 응답 확인. 슬라이스 2 의 측정 데이터가 전부 여기서 나왔다
- **단위 테스트** (슬라이스 3부터 적용): ADF 평문 변환은 실패하는 테스트를 먼저 커밋한 뒤
  통과 커밋을 쌓는다. 스펙을 먼저 확정하기 위함이다
- **공식 문서 대조**: 에이전트가 플랫폼 동작을 주장하면 문서로 확인시킨다. 4.2 가 그 사례다
- **결정 설명**: 코드를 쓸 때 결정마다 이유를 설명하게 하고 슬라이스 기록에 남긴다.
  나중에 코드를 읽을 때 왜 그렇게 됐는지 추적하기 위함이다
