# 설계 결정

구현보다 먼저 쓴다. 결정이 바뀌면 갱신하고 왜 바뀌었는지 남긴다.
각 결정에는 근거를 붙이고, 기각한 대안이 있으면 기각 이유도 적는다.

## 0. 설계 방법

설계를 확정하기 전에 빈 앱을 데모 사이트에 배포하고 Confluence API 를 직접 호출해 응답을
확인했다. 문서만 읽고 세운 초안이 실제와 다른 경우가 있어서, 코드를 쓰기 전에 입력의
실물을 보기로 했다.

측정으로 초안이 바뀐 지점은 다섯 군데다.

| 문서만 보고 세운 가정 | 실제 |
|---|---|
| 태스크 body 가 ADF 객체로 온다 | JSON 문자열로 온다. 한 번 더 풀어야 한다 |
| 한 줄이 조각 하나다 | 서식이 바뀔 때마다 쪼개진다. 이어붙이지 않으면 제목이 잘린다 |
| 모든 조각에 글자가 들어 있다 | 멘션, 이모지, 링크 카드에는 `text` 필드가 없다 |
| `localId` 로 태스크를 식별한다 | 형식이 일정하지 않다. 전역 고유한 `id` 를 써야 한다 |
| 태스크에는 내용이 있다 | 회의록 템플릿의 빈 체크박스가 `placeholder` 조각으로 온다 |

다섯 개 모두 변환 로직의 입력 정의를 바꾼다. 측정하지 않았다면 구현 중에 발견해서
설계를 다시 썼을 것이다.

그래서 이 문서는 측정으로 확인한 항목에 `실측` 을 표시하고, 문서 근거만 있는 항목과
구분한다. 측정 과정과 원본 데이터는 `AI_USAGE.md` 슬라이스 2 에 있다.

## 1. 스택과 구조

### 1.1 전체 구조

두 방향의 흐름이 있다. 정방향은 사람이 시작하고, 역방향은 이벤트가 시작한다.

```
[정방향] 회의록의 액션 아이템을 Jira 이슈로

Confluence 페이지 더보기 메뉴
  └─ 모달 (UI Kit)
       ├─ 리졸버 GET /wiki/api/v2/tasks?page-id={id}&status=incomplete
       │                              &body-format=atlas_doc_format
       ├─ ADF 트리를 이슈 제목 평문으로 변환        <- 직접 구현하는 핵심 로직
       ├─ 사용자가 대상 선택, 제목과 프로젝트 조정   <- 사용자 액션
       └─ 리졸버 POST /rest/api/3/issue
            └─ storage 에 { taskId: issueKey } 저장

[역방향] Jira 이슈 완료를 회의록에 반영

Jira 이슈 완료 → trigger avi:jira:issue:updated
  └─ storage 조회 → PUT /wiki/api/v2/tasks/{id} status=complete
```

Forge 모듈 구성은 아래와 같다.

| 모듈 | 역할 |
|---|---|
| `confluence:contentAction` | 페이지 더보기 메뉴에 진입점을 만들고 모달을 연다 |
| `function` (resolver) | 화면이 부르는 백엔드. 제품 REST 호출과 저장을 담당 |
| `trigger` | Jira 이슈 변경 이벤트를 받아 역방향을 실행 |

### 1.2 UI Kit 을 쓴다

**결정**: 화면을 `@forge/react` (UI Kit) 로 만든다. Custom UI 를 쓰지 않는다.

**근거**: 이번 화면은 모달 안의 목록과 체크박스, 입력 필드 수준이다. Custom UI 는 iframe
브리지 왕복, 높이 조절, Atlassian 디자인 재현에 시간이 든다. 처음 다루는 플랫폼에서
그 시간은 설계와 문서에 쓰는 편이 낫다.

**대가**: 컴포넌트가 `@forge/react` 가 내보내는 것으로 제한된다. HTML 태그를 쓰면 렌더 자체가
안 되고, `Table` 대신 `DynamicTable` 처럼 이름이 다른 것도 있다. 이번 화면 요구사항은
그 안에서 충분히 해결된다.

### 1.3 페이지 본문이 아니라 tasks API 를 쓴다

**결정**: 액션 아이템을 `GET /wiki/api/v2/tasks` 로 읽고, 상태 변경은 `PUT /wiki/api/v2/tasks/{id}`
로 한다. 페이지 본문(storage 또는 ADF)을 직접 읽거나 쓰지 않는다.

**근거**: tasks API 가 `page-id` 필터와 페이지네이션을 제공하고, 상태만 따로 바꿀 수 있다.
본문을 다시 쓰는 방식이면 동시 편집 충돌과 버전 히스토리 오염을 직접 감당해야 한다.
회의록은 여러 사람이 동시에 여는 문서라 이 위험이 실제로 발생한다.

**실측**: `page-id` 필터와 `body-format=atlas_doc_format` 이 기대대로 동작한다. 응답의 태스크
객체에 `id`, `localId`, `status`, `pageId`, `spaceId`, `assignedTo`, `body`, `dueAt` 이 들어온다.

### 1.4 제품 REST 호출을 리졸버에 둔다

**결정**: 화면에서 `requestConfluence` 로 직접 호출하지 않고, 리졸버를 거친다.

**근거**: 두 가지다. 첫째, 매핑 저장이 필요한데 Forge Storage 는 클라이언트에서 호출할 수
없고 백엔드에서 `.asApp()` 으로만 접근된다. 둘째, 역방향 트리거도 같은 호출을 쓰기 때문에
로직을 한 곳에 두면 중복이 없다.

**기각한 대안**: Atlassian 이 배포한 Forge 에이전트 지침은 프론트에서 `requestConfluence` 를
직접 부르는 쪽을 기본으로 권한다. 단순한 조회 앱에는 맞는 조언이지만, 저장과 트리거가 있는
이 앱에는 맞지 않는다. 백엔드 로직을 비워두게 되는 문제도 있다.

### 1.5 매핑은 태스크 id 를 키로 저장한다

**결정**: Forge Key-Value Storage 에 `task:{taskId}` 형태로 Jira 이슈 키를 저장한다.
`localId` 를 키로 쓰지 않는다.

**근거**: **실측**으로 `localId` 의 형식이 일정하지 않다는 것을 확인했다. 시드 데이터의 태스크는
`1` 이었고, 새로 만든 태스크는 UUID(`a4737eb0-...`)였다. 반면 `id` 는 사이트 전역에서 고유하다.
매핑 키는 `id` 여야 한다.

**주의**: `id` 는 `"196"` 처럼 숫자로 보이지만 API 가 문자열로 준다. 슬라이스 4 에서 정수로
단정했다가 테스트가 잡았다. 비교와 저장 키 모두 문자열로 다룬다.

역방향에서 Jira 이슈로부터 태스크를 찾아야 하므로 반대 방향 키도 함께 저장한다.

```
task:{taskId}    -> { issueKey, pageId, createdAt }
issue:{issueKey} -> { taskId, pageId }
```

**패키지**: `@forge/api` 의 `storage` 는 폐기 예정이라 `@forge/kvs` 의 `kvs` 를 쓴다.
`get`, `set`, `delete` 로 인터페이스가 같아서 import 만 바꾸면 된다. `forge lint` 가
경고로 알려준다.

### 1.6 담당자는 accountId 를 그대로 넘긴다

**결정**: 태스크의 `assignedTo` 를 Jira 이슈 담당자로 그대로 쓴다. 별도 사용자 매핑 테이블을
두지 않는다.

**근거**: **실측**으로 검증했다. 액션 아이템 안에 사람을 멘션하면 `assignedTo` 에 Atlassian
accountId 가 채워지고, 그 값이 같은 사이트 Jira 계정 id 와 동일하다.

```
멘션 조각 attrs.id  712020:a5756fbe-2fc1-4ad1-af73-2d97a40e8670
assignedTo          712020:a5756fbe-2fc1-4ad1-af73-2d97a40e8670
```

**단서**: `assignedTo` 가 비어 있는 태스크가 흔하다. 측정한 5건 중 4건이 비어 있었다.
담당자 없이 이슈를 만들 수 있어야 한다.

**실측**: 슬라이스 7 에서 실제로 이슈를 만들어 담당자가 붙는 것을 확인했다. 회의록에서
멘션한 사람이 Jira 이슈 담당자로 그대로 들어간다. 사용자 매핑 테이블이 필요 없다.

### 1.7 ADF 평문 변환 규칙

이 앱에서 직접 구현하는 핵심 로직이다. 라이브러리가 대신 해주지 않는다.

**입력**: `body.atlas_doc_format.value`. **실측**으로 이 값이 객체가 아니라 JSON 문자열이라는
것을 확인했다. 한 번 더 풀어야 한다.

**구조**: `doc > content[] > paragraph > content[]` 아래에 조각(node)이 여러 개 들어온다.
서식이 바뀌는 지점마다 조각이 쪼개진다. 화면에서 한 줄로 보여도 데이터는 여러 개다.

```
"굵게 와 기울임 섞기"  ->  [ {"굵게", marks:[strong]}, {" 와 "},
                            {"기울임", marks:[em]}, {" 섞기"} ]
```

**규칙**:

```
1. value 를 JSON 으로 푼다
2. 조각을 순서대로 순회하며 글자를 뽑는다
      text        node.text          marks 는 버린다
      mention     attrs.text         "@Deokhyeon Eom"
      emoji       attrs.text 또는 attrs.shortName
      inlineCard  attrs.url          글자 필드가 아예 없다
      placeholder 건너뛴다            내용 없는 빈 체크박스
      hardBreak   공백 하나로
3. 인라인 조각끼리는 구분자 없이 붙이고, 블록 사이에만 공백 하나를 넣는다
4. 연속 공백을 하나로 줄인다
5. 앞뒤 공백을 자른다
6. 결과가 비면 그 태스크는 목록에서 제외한다
7. 255자를 넘으면 자르고 말줄임을 붙인다
```

3번의 근거가 실측에 있다. 공백이 이미 text 조각 안에 들어 있다 (`' 와 '`). 인라인 사이에
구분자를 넣으면 서식이 단어 중간에 걸렸을 때 `굵은글씨` 가 `굵은 글씨` 로 갈라진다.

4번이 필요한 이유도 실제 데이터에 있다. 링크와 멘션 다음 조각이 공백 두 개로 시작한다.

6번이 필요한 이유도 실제 데이터에 있다. 회의록 템플릿의 빈 체크박스는 `placeholder` 조각만
가진다. 거르지 않으면 `Type /action item to add and assign tasks.` 라는 제목의 이슈가 생긴다.

`emoji` 와 `inlineCard` 는 실물을 보지 못했다. ADF 문서를 근거로 처리하고 단위 테스트로 덮는다.

### 1.8 대상은 Confluence, Jira 는 연동 대상

**결정**: 앱의 진입점과 사용자 화면은 Confluence 에만 둔다. Jira 에는 화면을 두지 않고
이슈 생성과 이벤트 수신만 한다.

**근거**: 이 앱이 푸는 문제는 회의록에서 시작한다. 사용자가 앱을 여는 곳도 회의록이다.
Jira 쪽에 화면을 더하면 같은 일을 두 곳에서 할 수 있게 되어 오히려 혼란스럽다.

두 제품을 오가는 것이 주제의 핵심이라 한쪽만으로는 성립하지 않는다. 다만 무게중심은
Confluence 에 있다.

**manifest 표현**: 이 판단이 `app.compatibility` 에 그대로 드러난다. Forge 는 정확히 하나를
`required: true` 로 요구하므로, 주 제품인 Confluence 를 필수로, Jira 를 선택으로 둔다.

```yaml
compatibility:
  confluence:
    required: true
  jira:
    required: false
```

**대가**: Jira 가 없는 사이트에도 설치된다. 그러면 조회는 되고 이슈 생성만 막히므로 화면이
그 상태를 다뤄야 한다 (1.11).

**설치**: 스코프가 두 제품에 걸치면 제품마다 따로 설치해야 한다. Confluence 설치만으로는
Jira 를 부를 수 없다.

```
forge install --product confluence   # 주 제품
forge install --product jira         # 연동 대상
```

### 1.9 단일 사이트로 제한된다

**결정**: 같은 Atlassian 사이트 안의 Confluence 와 Jira 만 잇는다. 다른 사이트의 Jira 로는
이슈를 만들지 않는다.

**근거**: Forge 앱은 설치된 사이트 안에서만 제품 API 를 호출한다. 사이트를 넘으려면 외부
연동과 자격증명 보관이 필요해지고, 그건 이 주제의 범위를 넘는다.

이 제약이 문제가 되지 않는 이유는, 회의록과 그 회의록이 낳는 이슈가 보통 같은 조직 안에
있기 때문이다. 담당자 매핑이 필요 없는 것도 같은 이유다 (1.6).

### 1.10 변환 함수의 경계

**결정**: 1.7 의 변환을 두 함수로 나눈다.

```
toPlainText(adfNode)    ADF 노드 트리 → 글자 한 줄. Confluence 도 Jira 도 모른다
titleFromTask(task)     Confluence 태스크 객체 → Jira 제목, 비어 있으면 null
```

**근거**: 변경 이유가 다르다. ADF 에 새 노드 종류가 생기면 앞의 것만 바뀌고, Jira 제목
제한이 바뀌거나 입력이 태스크가 아닌 다른 것이 되면 뒤의 것만 바뀐다. 테스트도 갈린다.
앞은 손으로 만든 최소 ADF 로 노드 종류별 규칙을 검증하고, 뒤는 실측 태스크 객체로 끝에서
끝까지를 검증한다.

**세부**:

- `titleFromTask` 가 JSON 문자열 파싱, 빈 결과의 `null`, 길이 제한을 맡는다. 1.7 의 규칙 1, 6, 7 이다
- 말줄임은 `…` 한 글자. `...` 보다 두 글자를 더 살린다
- 제한 상수 `TITLE_MAX_LENGTH` 를 내보내 테스트와 화면이 같은 값을 쓰게 한다
- 테스트 데이터는 지어내지 않는다. 실측 응답 6건을 `src/lib/fixtures/tasks.json` 에 그대로
  둔다. 통과하면 실제 데이터에서 동작한다는 뜻이 된다
- 테스트 파일은 소스 옆에 둔다. Forge 번들러는 핸들러에서 import 되는 파일만 묶으므로
  배포본에 딸려가지 않는다. 슬라이스 5 배포에서 확인했다
- 멘션은 `@이름` 을 제목에 남긴다. 회의록 문장과 제목이 같아야 찾기 쉽고, 담당자는 별도
  필드로도 들어가므로 정보가 겹쳐도 해롭지 않다

### 1.11 리졸버 계약과 화면 상태

**결정**: 리졸버는 화면이 그대로 그릴 수 있는 모양으로 돌려준다. 화면은 ADF 를 모른다.

```
invoke('getTasks') → {
  pageId,                                  현재 페이지
  tasks: [{ id, title, assignedTo }],      변환이 끝난 항목만
  skipped,                                 내용이 비어 제외한 개수
  error,                                   오류 코드 또는 null
}
```

**근거**: 변환 로직이 화면으로 새어나가면 슬라이스 4 의 테스트가 지키는 범위를 벗어난다.
`titleFromTask` 를 리졸버에서 끝내면 화면이 바뀌어도 변환 규칙은 테스트가 계속 지킨다.

**세부**:

- **`skipped` 를 센다.** 조용히 버리면 사용자가 회의록의 개수와 목록의 개수가 다른 이유를
  알 수 없다. `빈 항목 2건 제외` 로 알린다
- **오류를 던지지 않고 담아 보낸다.** 리졸버가 throw 하면 Forge 기본 오류 화면이 떠서
  안내 문구를 정할 수 없다. 코드로 받아 화면이 상황에 맞는 문장을 그린다
- **조회는 `asUser()` 로 한다.** 볼 수 없는 페이지의 태스크는 응답에 들어오지 않는다.
  권한 검사 코드를 직접 쓰지 않아도 된다
- **페이지 id 는 `req.context` 에서 받는다.** Atlassian 이 채워 넘기므로 사용자가 위조할 수
  없고, 지금 열려 있는 페이지의 태스크만 본다는 것이 보장된다
- **기본값은 전체 선택.** 옮기려고 여는 화면이라 빈 목록에서 하나씩 고르게 하지 않는다

**화면 상태**: 빈 상태를 셋으로 나눈다. 사용자가 할 일이 다르기 때문이다.

| 상태 | 안내 | 사용자가 할 일 |
|---|---|---|
| 로딩 | 불러오는 중 | 기다린다 |
| 목록 있음 | 체크박스 목록, 제외 개수 | 고른다 |
| 태스크 0건 | 미완료 액션 아이템이 없다 | 회의록에 항목을 적는다 |
| 전부 제외됨 | 빈 항목 n건 제외 | 회의록에서 내용을 채운다 |
| 권한 오류 | 볼 권한이 없다 | 공간 관리자에게 문의한다 |
| 그 외 오류 | 잠시 후 다시 시도 | 재시도한다. 상세는 로그로 |

오류 문구는 원인이 아니라 다음 행동을 적는다. `403 Forbidden` 은 사용자에게 아무것도
알려주지 않는다.

**Jira 쪽 상태**: 1.8 의 `required: false` 때문에 Jira 가 없는 사이트에도 설치된다.

| 상태 | 안내 |
|---|---|
| Jira 없음 또는 권한 없음 | 이 사이트에 Jira 가 없거나 접근 권한이 없다 |
| 프로젝트 0건 | 이슈를 만들 수 있는 프로젝트가 없다 |
| 이슈 타입 조회 실패 | 다른 프로젝트를 골라달라 |
| 제목 255자 초과 | 해당 입력창에 현재 글자 수와 함께 표시 |

Jira 가 404 를 주는 경우와 403 을 주는 경우를 같은 안내로 묶는다. 사용자가 할 일이
관리자에게 문의하는 것으로 같기 때문이다.

**읽기와 쓰기를 화면에서 나눈다**: 목록 조회, 제목 수정, 항목 선택은 프로젝트를 고르기 전에도
할 수 있다. 되돌릴 수 있는 일이고, 옮길 것이 있는지 먼저 봐야 프로젝트를 고를 이유가 생긴다.
되돌릴 수 없는 이슈 생성만 버튼에서 막는다. 잠금을 목록이 아니라 버튼에 건다.

### 1.12 이슈 타입은 untranslatedName 으로 고른다

**결정**: 사용자에게 이슈 타입을 고르게 하지 않는다. 리졸버가 아래 순서로 하나를 정하고
화면은 결과만 표시한다.

```
untranslatedName === 'Task'        번역 전 이름. 언어와 무관하다
name === 'Task'                    위가 없는 경우의 대비
hierarchyLevel === 0               표준 타입 (작업, 스토리, 버그)
목록의 첫 번째
```

**근거**: 회의록의 액션 아이템은 성격이 전부 할 일이다. Bug 나 Epic 으로 만들 이유가 없어서
선택지를 노출하는 대가로 얻는 것이 없다.

**실측**: 이슈 타입 이름은 보는 사람의 언어로 번역돼서 온다. 한국어 사이트에서 `Task` 는
`작업` 이다. `untranslatedName` 이 함께 오는 것을 확인했다.

```
id=10002  name='작업'       untranslatedName='Task'         hierarchyLevel=0
id=10003  name='하위 작업'   untranslatedName='Sub-task'     hierarchyLevel=-1
id=10004  name='새 기능'     untranslatedName='New Feature'  hierarchyLevel=0
```

`name` 만 보고 고르면 영어가 아닌 사이트에서 매칭이 실패해 목록의 첫 번째로 떨어진다.
시드 데이터에는 `[System] Incident` 나 `Access` 같은 타입이 섞여 있어서, 순서가 다른
프로젝트였다면 회의록의 할 일이 장애 보고서로 만들어졌을 것이다.

**하위 작업 제외**: 부모 이슈가 있어야 만들 수 있으므로 대상이 아니다. `subtask` 와
`hierarchyLevel === -1` 을 함께 본다.

**선택 결과를 화면에 표시한다.** 자동으로 정하는 값이라 무엇이 정해졌는지 보이지 않으면
사용자가 엉뚱한 타입으로 만들어지는 것을 알 수 없다.

### 1.13 이슈 생성은 전부 시도하고 결과를 항목별로 보고한다

여기서부터 되돌릴 수 없는 일이다. 설계의 무게가 무엇을 만드는가가 아니라 잘못됐을 때
어떻게 되는가에 실린다.

**결정**: 한 건이 실패해도 나머지를 계속 만든다. 성공과 실패를 항목별로 보여준다.

**기각한 대안**:

- **전부 아니면 전무**: 하나라도 실패하면 만든 이슈를 지운다. 이슈 삭제 권한을 추가로
  요구해야 하고, 그 권한을 요구하면 관리자가 설치를 꺼린다. 지우는 도중 실패하면 상태가
  더 엉킨다
- **첫 실패에서 중단**: 뒤의 항목은 시도조차 안 되어 사용자가 왜 안 만들어졌는지 모른다

**근거**: 이슈 생성 실패의 원인은 항목마다 다르다. 담당자가 그 프로젝트 사용자가 아니거나,
제목이 규칙에 안 맞거나, 필수 커스텀 필드가 있는 경우다. **한 건의 실패가 나머지가
잘못됐다는 뜻이 아니다.** 되돌리면 멀쩡한 항목까지 날린다.

**담당자 실패는 담당자를 빼고 재시도한다.** 회의록에서 멘션된 사람이 그 Jira 프로젝트를
못 쓰는 경우가 흔하다. 사용자가 원하는 것은 할 일이 Jira 에 있는 것이지 담당자가 정확히
붙는 것이 아니다. 이슈만 있으면 담당자는 Jira 에서 한 번에 고치지만, 이슈가 없으면
회의록으로 돌아가 다시 옮겨야 한다. 화면에 `담당자 지정 실패` 로 표시한다.

**순차로 보낸다.** 병렬은 Jira 속도 제한에 걸릴 수 있고, 결과 순서가 입력과 같아야 화면이
단순해진다. 한 회의록에 몇 건 수준이라 체감 차이가 없다.

**매핑 저장 실패를 결과에 드러낸다.** 이슈는 만들어졌는데 매핑이 빠지면 역방향이 조용히
끊긴다. 조용한 실패가 가장 나쁘다.

**제목 길이를 리졸버에서 다시 검사한다.** 화면에서 이미 막지만 화면 검증만 믿으면 안 된다.

### 1.14 이미 옮긴 항목은 표시하되 막지 않는다

**결정**: `getTasks` 가 매핑을 함께 조회해 이미 이슈가 있는 항목에 이슈 키를 표시하고
기본 선택에서 뺀다. 다시 만드는 것을 막지는 않는다.

**근거**: 사용자가 모달을 두 번 열면 이슈가 두 개 생긴다. 실제로 일어나는 결함이다.
다만 일부러 두 번 만들 이유가 있을 수 있고, 앱이 사용자를 막을 만큼 확신할 근거가 없다.
기본값으로 유도하되 결정은 사용자에게 남긴다.

**부수 효과**: 이것이 매핑 저장을 화면에서 확인하는 유일한 수단이다. Forge Storage 는
눈으로 볼 수 없어서, 모달을 다시 열었을 때 이슈 키가 뜨는 것으로 저장을 확인한다.

**매핑 조회가 실패해도 목록은 보여준다.** 중복 표시가 없을 뿐 조회 자체는 성립한다.

### 1.15 되돌릴 수 없는 일만 버튼에서 막는다

목록 조회, 제목 수정, 항목 선택은 프로젝트를 고르기 전에도 할 수 있다. 되돌릴 수 있는
일이고, 옮길 것이 있는지 먼저 봐야 프로젝트를 고를 이유가 생긴다.

| 조건 | 버튼 |
|---|---|
| 프로젝트 미선택 | 비활성. 대상 프로젝트를 선택해달라 |
| 이슈 타입 미확정 | 비활성 |
| 선택 항목 0건 | 비활성 |
| 제목이 255자 초과인 항목 있음 | 비활성 |
| 생성 중 | 비활성 |

**잠긴 이유를 버튼 아래에 적는다.** 회색 버튼만 있으면 무엇을 해야 풀리는지 모른다.

**생성 중 비활성화가 특히 중요하다.** 두 번 누르면 이슈가 두 배로 만들어진다.

### 1.16 이슈 본문에 회의록 링크를 넣는다

**결정**: 이슈 본문에 회의록으로 돌아가는 링크를 넣는다.

**근거**: 매핑은 Forge Storage 안에만 있어서 사람 눈에 보이지 않는다. 역방향 반영은 그것으로
되지만, Jira 에서 이슈를 본 사람이 원본 회의록으로 건너갈 방법이 없었다. 어느 회의에서
나왔는지, 무슨 맥락인지 알 수 없다.

두 곳을 잇는 것이 이 앱의 주제인데 정작 사람이 건널 다리가 없는 셈이었다. 설계 문서에
본문에 대한 결정 자체가 없었고, 구현하면서 빠뜨린 것을 화면 확인 중에 발견했다.

**여기서 ADF 를 처음으로 쓴다.** Jira v3 API 의 `description` 은 평문이 아니라 ADF 를 받는다.
1.7 에서 읽던 형식을 이번에는 만들어서 보낸다.

```
1.7      ADF → 평문     Confluence 태스크를 읽어 이슈 제목으로
1.16     평문 → ADF     Jira 이슈 본문을 만들어 쓰기
```

**단위 테스트를 붙인다.** ADF 모양이 틀리면 Jira 가 400 으로 거절하는데, 그러면 본문만
빠지는 것이 아니라 **이슈 생성 자체가 실패**한다. 결과 화면에는 `생성 실패` 로만 뜨고
원인이 보이지 않는다. 화면으로 확인할 수 없는 종류라 테스트로 고정한다.

**페이지 조회는 요청당 한 번이다.** 항목마다 부르지 않는다. 5건을 만들어도 페이지는 한 번만
읽는다.

**조회가 실패해도 이슈는 만든다.** 링크 없이 안내 문장만 남는다. 본문 때문에 이슈 생성을
막을 이유가 없다. 주소는 `_links.webui` 로 만들고, 그것을 못 받으면 `pageId` 로 여는
옛 주소로 되돌린다.

## 2. 범위

### 2.1 목표 범위와 구현 현황

만들려는 기능과 현재 상태다. 슬라이스가 끝날 때마다 이 표를 갱신한다.
작업이 끝나면 이 표가 그대로 무엇을 만들었고 무엇을 남겼는지에 대한 답이 된다.

| 기능 | 상태 | 커밋 |
|---|---|---|
| Forge 앱 스캐폴드와 배포 왕복 | 완료 | `00d2269` |
| 플랫폼 실측과 설계 확정 | 완료 | `06ee0c4` |
| ADF 평문 변환 스펙 (실패하는 테스트) | 완료 | `f5e28e4` |
| ADF 평문 변환 구현 | 완료 | `7e91bcd` |
| 페이지의 미완료 액션 아이템 조회 | 완료 | `cc434d5` |
| 목록 화면. 대상 선택 | 완료 | `cc434d5` |
| 목록 화면. 제목 수정, 프로젝트와 이슈 타입 선택 | 완료 | `2bb832f` |
| Jira 이슈 생성과 담당자 승계 | 완료 | `b20dd52` |
| 태스크와 이슈 매핑 저장 | 완료 | `b20dd52` |
| 이미 옮긴 항목 표시 | 완료 | `b20dd52` |
| 이슈 본문에 회의록 링크 | 완료 | 이 커밋 |
| Jira 이슈 완료 시 Confluence 태스크 상태 반영 | 미착수 | |

### 2.2 하지 않기로 한 것

판단해서 뺀 것들이다. 시간이 모자라 못 하게 되는 것이 생기면 그 사실과 함께 구분해서 적는다.

- **역방향의 반대 (Confluence 체크 → Jira 이슈 완료)**: Confluence 태스크 상태 변경을
  받을 수 있는 Forge 이벤트가 없다. 주기적 폴링으로 흉내낼 수는 있지만 비용 대비 얻는
  것이 적다.
- **이미 이슈가 있는 태스크의 재생성 방지 이상의 동기화**: 제목이나 담당자가 나중에
  달라져도 맞추지 않는다. 한 번 옮긴 뒤에는 Jira 가 단일 출처다.
- **Jira 쪽 화면**: 1.8 에 적은 이유.
- **리졸버와 트리거의 단위 테스트**: Forge SDK 목킹에 드는 시간 대비 얻는 것이 적다.
  실제 배포본에서 확인한다.

### 2.3 알려진 한계

- **태스크를 지우고 다시 쓰면 매핑이 끊긴다.** **실측**으로 확인했다. 액션 아이템의 내용을
  지우고 새로 쓰면 태스크 `id` 가 바뀐다 (`197` 이 사라지고 `201` 이 생겼다). 서식만 바꾸면
  id 는 유지된다. 매핑이 끊기면 역방향 반영이 조용히 실패한다.
- **앱 자격으로 보이는 태스크가 제한된다.** **실측**으로 확인했다. 같은 요청을 사용자 자격과
  앱 자격으로 보냈을 때, 특정 페이지에서는 둘 다 5건이었지만 사이트 전체로는 사용자 61건,
  앱 5건이었다. 역방향 트리거는 앱 자격으로 돌기 때문에 스페이스 권한 설정에 따라 반영이
  안 될 수 있다.
- **`emoji` 와 `inlineCard` 는 문서 근거로만 처리한다.** 실물 응답을 보지 못했다.
- **한 페이지의 액션 아이템을 100건까지만 읽는다.** tasks API 가 페이지네이션을 주지만
  따라가지 않는다. 회의록 하나에 액션 아이템이 100건을 넘는 경우를 상정하지 않았다.
- **데모 사이트는 2026-11-23 에 만료된다.**

## 3. 개선 방향

지금 구조에서 다음에 붙일 수 있는 것들이다. 현재 한계에서 출발한다.

- **매핑이 끊긴 태스크 알리기**: 2.3 의 첫 항목 때문에 역방향이 조용히 실패할 수 있다.
  이슈가 완료됐는데 매핑된 태스크를 못 찾으면 이슈에 댓글로 남기면 사용자가 알 수 있다.
- **앱 권한 진단 화면**: 2.3 의 두 번째 항목은 설치 환경에 따라 갈린다. 모달에서 앱 자격으로
  태스크가 몇 건 보이는지 표시하면 관리자가 원인을 바로 안다.
- **회의록 템플릿 대응**: 빈 `placeholder` 태스크를 지금은 거르기만 한다. 회의록에서
  채워지지 않은 액션 아이템이 몇 개인지 알려주면 회의 진행에 쓸모가 있다.
