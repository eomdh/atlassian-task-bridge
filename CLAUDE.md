# Task Bridge

Atlassian Forge 앱. Confluence 회의록에 흩어진 액션 아이템을 Jira 이슈로 일괄 생성하고,
그 이슈가 완료되면 원래 Confluence 태스크 상태에 되돌려 반영한다.
같은 사이트의 Jira 와 Confluence 를 잇는 단일 앱이다.

## 스택

임의로 바꾸지 않는다. 변경이 필요하면 먼저 묻는다.

- Forge 런타임 `nodejs24.x`, 아키텍처 arm64
- UI Kit (`@forge/react`). Custom UI 를 쓰지 않는다
- `@forge/resolver` 백엔드 리졸버, `@forge/bridge` 프론트 호출
- Forge Key-Value Storage (태스크와 이슈 매핑)
- 바닐라 JavaScript. 템플릿이 JS 이고 TypeScript 로 옮기지 않는다
- Vitest (ADF 변환 단위 테스트)

## 명령어

```
npm test                                         단위 테스트 1회 실행
npm run lint                                     ESLint
forge lint                                       manifest 와 코드 검증
forge install list                               현재 설치 목록과 installationId
forge deploy --non-interactive -e development    배포
forge install --non-interactive --upgrade ...    스코프 변경 후 재설치
forge logs --since 15m -e development            배포된 앱 로그
forge tunnel                                     로컬 코드로 프록시
```

- 모든 명령은 `manifest.yml` 이 있는 레포 루트에서 실행한다
- `deploy`, `install`, `environments` 에는 `--non-interactive` 를 붙인다. 안 붙이면 프롬프트가 떠서 비대화형 셸에서 막힌다
- 자격증명과 사이트 정보는 `CLAUDE.local.md` 에 있다
- `npm test` 는 ADF 변환 스펙을 처음 쓰는 슬라이스에서 Vitest 와 함께 들어온다. 그 전까지는 없다
- **`forge uninstall` 은 `--non-interactive` 를 안 받는다.** `forge install list` 로 installationId 를
  받아 `forge uninstall -i <id>` 로 부르면 프롬프트가 없다
- **설치와 삭제 작업은 서버에서 하나씩 처리되고 20분 넘게 걸릴 수 있다.** 끝나기 전에 다시 보내면
  `conflicts with another pending or in progress task` 가 난다. 그때는 기다리는 것 말고 할 일이 없다.
  실패했다고 바로 재시도하면 큐만 더 꼬인다
- **프롬프트가 필요한 명령은 이 세션에서 못 돌린다.** `!` 를 붙여도 TTY 가 아니다. 별도 터미널을 연다

## 완료 기준

작업을 마쳤다고 보고하기 전에 통과시킨다.

```
npm test && forge lint
```

이 둘로도 부족하다. 화면이나 트리거를 건드렸으면 **데모 사이트에 배포해서 실제로 동작하는지 확인한다.**
`forge lint` 는 manifest 문법과 알려진 오류만 본다. UI Kit 컴포넌트를 잘못 써서 렌더가 안 되는 것은 잡지 못한다.

에이전트가 명령을 돌려 통과했다고 보고하는 것으로 슬라이스를 닫지 않는다.
**사람이 직접 실행해서 눈으로 확인한 뒤에 커밋한다.** 무엇을 어떻게 확인했는지는
`AI_USAGE.md` 해당 슬라이스의 `직접 확인` 에 남긴다. 확인하지 않은 것이 있으면 그것도 적는다.

TODO 주석, 미구현 분기, 빈 catch, `test.skip`, `test.only` 를 남긴 채 완료로 보고하지 않는다.

## 테스트

핵심 로직인 ADF 평문 변환만 단위 테스트한다. 입력이 ADF 트리, 출력이 문자열인 순수 함수라
Forge SDK 목킹 없이 검증할 수 있다.

- **실패하는 테스트를 먼저 커밋한 뒤 통과 커밋을 쌓는다** (red/green 쌍). 스펙을 먼저 확정하기 위함
- 리졸버와 트리거는 단위 테스트하지 않는다. 실제 배포본에서 확인한다.
  Forge SDK 목킹에 드는 시간 대비 얻는 것이 적다
- 경계 조건을 빠뜨리지 않는다. mention, 링크, 이모지, 중첩 마크, 빈 body, 제목 길이 제한

## 작업 방식

- **슬라이스 하나 = 커밋 하나.** 여러 관심사를 한 커밋에 섞지 않는다
- **슬라이스를 시작하기 전에 범위와 방식을 제안하고 승인을 받는다.** 코드부터 쓰지 않는다
- **설계 결정은 코드보다 먼저.** `DECISIONS.md` 에 근거를 적고 나서 구현한다
- 핵심 로직인 ADF 평문 변환은 설계를 먼저 합의하고 구현한다
- 한 가지 수정이 두세 번 실패하면 멈추고 상황과 대안을 보고한다

## 범위

아래 셋은 최소선이다. 시간이 모자라 기능을 덜어낼 때도 이 선 아래로 내려가지 않는다.

- 실제로 동작하는 최소 기능. 다듬기보다 왕복이 끝나는 것이 우선
- UI 와 백엔드 로직을 둘 다 가진다. 프론트에서 REST 를 직결해 리졸버를 비워두지 않는다
- 조회, 입력, 저장, 상태 변경 중 사용자 액션이 최소 하나 실제로 동작한다

위로는 늘리지 않는다.

- 요구하지 않은 기능을 추가하지 않는다. 필요해 보이면 먼저 묻는다
- 스코프를 임의로 늘리지 않는다. 관리자가 설치할 때 이 목록을 그대로 본다
- 측정용 임시 코드는 목적과 제거 시점을 주석에 남긴다

## Forge 플랫폼

### UI Kit

- `@forge/react` 가 내보내는 컴포넌트만 쓴다. `<div>`, `<strong>` 같은 HTML 태그를 쓰면 렌더 자체가 안 된다
- `Table` 이라는 컴포넌트는 없다. `DynamicTable` 을 쓴다
- `@forge/ui` 는 폐기됐다. import 하면 앱이 깨진다
- `AdfRenderer` 로 ADF 를 변환 없이 그대로 그릴 수 있다

### 저장소

- Forge Storage 는 클라이언트에서 못 부른다. 백엔드 리졸버에서 `.asApp()` 로만 접근한다
- 태스크와 이슈 매핑 저장이 리졸버를 거쳐야 하는 이유가 이것이다

### 인증

- 리졸버의 제품 REST 호출은 `asUser()` 를 기본으로 한다. 권한 검사를 제품이 대신 해준다
- 사용자 컨텍스트에서 `asApp()` 을 쓰면 권한 검사를 직접 해야 한다

### 배포

- manifest 를 고쳤으면 배포 전에 `forge lint` 로 검증한다
- 린트는 import 해석을 하지 않는다. 없는 패키지를 import 해도 통과하고 번들링에서 실패한다
- 스코프나 egress 를 추가하면 재배포 후 재설치까지 해야 반영된다
- 터널 중에는 manifest 변경만 재배포가 필요하다. 코드 변경은 핫리로드된다

## 코드 규약

- 주석은 한글로 쓴다. 처음부터 한글로 쓰고 나중에 번역하지 않는다
- 주석은 왜만 짧게. 코드를 읽으면 아는 것은 적지 않는다
- 식별자와 스펙 용어는 영어 그대로 쓴다 (`AbortController`, `atlas_doc_format`)
- em dash(—)와 가운뎃점(·)을 쓰지 않는다. 쉼표나 괄호로 대체한다
- 회사명, 과제, 제출, 평가, 마감 같은 어휘를 코드와 문서와 커밋에 남기지 않는다

## 데모 환경과 다른 가정

개발용 데모 사이트에서만 통하는 가정은 `[데모 환경]` 표식이 붙은 주석으로 명시한다.
표식을 통일해야 `grep` 으로 전부 찾을 수 있다.

```js
// [데모 환경] 시드 데이터의 회의록 페이지를 기준으로 태스크를 찾는다.
// 실제 사용에서는 사용자가 연 페이지의 id 가 들어온다.
```

## 커밋

Conventional Commits 를 쓴다. 타입은 영어, 설명은 한글.
제목과 본문 모두 명사형 개조식으로 끝낸다.

```
feat: 태스크 ADF 를 이슈 제목 평문으로 변환 추가

mention 은 표시 이름으로, inlineCard 는 URL 로 눌러 한 줄로 만든다.
제목 길이 제한 255자를 넘으면 자르고 말줄임을 붙인다.
검증: 시드 회의록 3개 페이지에서 태스크 17건 변환, 실제 배포본에서 확인
```

- 커밋 하나에 결정 하나
- 본문에 검증 방법을 한 줄 남긴다
- 제목에 em dash 로 부제를 붙이지 않는다
- AI co-author 트레일러를 붙이지 않는다
- **커밋은 사람의 지시로 진행한다.** 알아서 커밋하지 않는다

## 문서

문서는 세 개만 둔다. 읽는 사람이 던지는 질문 여덟 개에 빠짐없이 답해야 하고,
어느 질문을 어느 문서가 맡는지는 아래가 단일 출처다. 역할을 겹치지 않게 한다.

| 질문 | 문서 |
|---|---|
| 무엇을 만들었고 왜 이 주제인가 | README.md |
| 어떤 문제를 푸는가 | README.md |
| 무슨 기능이 있는가 | README.md |
| 어떻게 실행하는가 | README.md |
| 어떤 스택이고 어떤 구조인가 | DECISIONS.md |
| 무엇을 구현했고 무엇을 남겼는가 | DECISIONS.md |
| 앞으로 무엇을 개선할 수 있는가 | DECISIONS.md |
| AI 를 어떻게 활용했는가 | AI_USAGE.md |

### README.md

읽는 사람이 처음 여는 문서다. 코드를 안 봐도 무엇인지 알 수 있어야 한다.

- 무엇을 만들었나: 한 문단. 이어서 왜 이 주제를 골랐는지
- 문제: 회의록의 액션 아이템과 Jira 이슈 사이에서 추적이 끊기는 지점.
  일반론이 아니라 구체적인 상황으로 쓴다
- 기능: 정방향과 역방향을 흐름도 하나로 보여주고, 화면에서 사용자가 하는 일을 적는다
- 실행: 설치부터 화면을 여는 데까지. 명령을 그대로 복사해 쓸 수 있게 한다.
  설치 없이 볼 수 있도록 데모 화면 기록을 함께 둔다
- 1인칭 회고 서술을 넣지 않는다. 무엇이고 어떻게 동작하는지만

### DECISIONS.md

번호 매긴 결정 목록. 각 결정에 근거를 붙인다. **구현보다 먼저 쓴다.**
결정이 바뀌면 갱신하고, 왜 바뀌었는지 남긴다.

담아야 할 것은 세 갈래다.

1. **스택과 구조**: Forge 모듈 구성, 데이터 흐름, 저장 스키마, 리졸버 경계.
   왜 UI Kit 인지, 왜 페이지 본문 파싱이 아니라 tasks API 인지 같은 선택 근거
2. **범위**: 구현한 것과 남긴 것. 남긴 것은 못 한 게 아니라 안 한 것이면
   그 판단 근거를 적는다. 시간 배분도 근거다
3. **개선 방향**: 지금 구조에서 다음에 무엇을 붙일 수 있는지.
   막연한 희망이 아니라 현재 한계에서 출발한다

아래 두 항목은 읽는 사람이 갸웃할 수 있으니 먼저 밝히고 근거를 댄다.

- Confluence 와 Jira 두 제품을 쓰는 이유. 대상은 Confluence 이고 Jira 는 연동 대상이라는 관점
- 단일 사이트로 제한되는 이유와 그 제약이 이 주제에 문제가 되지 않는 이유

### AI_USAGE.md

에이전트 기반으로 개발했다는 사실 자체보다 **어떻게 통제했는지**를 담는다.

- 사용한 도구와 모델, 적용한 작업 범위
- 사람과 에이전트의 역할 경계. 핵심 로직을 누가 소유했는지
- 슬라이스별 기록. 항목 순서를 고정한다
  ```
  지시       무엇을 요청했는지
  판단       어떤 안 중에 무엇을 왜 골랐는지
  산출       무엇이 나왔는지
  검증       에이전트가 돌린 명령과 결과
  직접 확인   사람이 직접 실행해 눈으로 본 것. 확인하지 않은 것도 적는다
  ```
- 에이전트가 틀린 사례와 그것을 어떻게 잡았는지
- 검증 장치: 테스트, 실제 배포 확인, 린트

끝에 재구성하지 않는다. 작업 시 해당 기록을 같은 커밋에 함께 담는다.
제안을 반려하거나 방향을 바꾼 경우도 그날 기록한다.
